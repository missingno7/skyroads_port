"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:0C26  (41 instructions, 14 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x0C26)
SIGNATURE = bytes.fromhex('c80400005657ff7608ff7606ff7604e8')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x0C26, 1: 0x0C38, 2: 0x0C49, 3: 0x0C4C, 4: 0x0C52, 5: 0x0C55, 6: 0x0C5B, 7: 0x0C5E, 8: 0x0C6F, 9: 0x0C78, 10: 0x0C82, 11: 0x0C85, 12: 0x0C8E, 13: 0x0C94}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:0C38": 1}


def lifted_1010_0c26(cpu, bb=0):
    """Lifted replacement for 1010:0C26."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x0C26, SIGNATURE, 'lifted_1010_0c26')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:0C26  c8040000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x4) & 0xFFFF
            # 1010:0C2A  56           push r16
            cpu.push(s.si)
            # 1010:0C2B  57           push r16
            cpu.push(s.di)
            # 1010:0C2C  ff7608       push rm16
            _o = (s.bp + 0x8) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:0C2F  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:0C32  ff7604       push rm16
            _o = (s.bp + 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:0C35  e888f8       call
            cpu.instruction_count += 7
            emulate_call(cpu, 0x1010, 0x04C0, 0x0C38)
            bb = 1
        elif bb == 1:
            # 1010:0C38  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:0C3B  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:0C3D  8bc6         mov
            s.ax = (s.si) & 0xFFFF
            # 1010:0C3F  25000f       and
            _a = s.ax
            _b = 0xF00
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0C42  8bf8         mov
            s.di = (s.ax) & 0xFFFF
            # 1010:0C44  83ff00       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0C47  7503         jnz
            cpu.instruction_count += 7
            bb = 3 if cpu.condition(0x5) else 2
        elif bb == 2:
            # 1010:0C49  e92c00       jmp
            cpu.instruction_count += 1
            bb = 9
        elif bb == 3:
            # 1010:0C4C  837e0a00     grp1 rm16,imm8sx
            _o = (s.bp + 0xA) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0C50  7403         jz
            cpu.instruction_count += 2
            bb = 5 if cpu.condition(0x4) else 4
        elif bb == 4:
            # 1010:0C52  e92300       jmp
            cpu.instruction_count += 1
            bb = 9
        elif bb == 5:
            # 1010:0C55  81ff0001     grp1 rm16,imm16
            _a = s.di
            _b = 0x100
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0C59  7503         jnz
            cpu.instruction_count += 2
            bb = 7 if cpu.condition(0x5) else 6
        elif bb == 6:
            # 1010:0C5B  e91100       jmp
            cpu.instruction_count += 1
            bb = 8
        elif bb == 7:
            # 1010:0C5E  8bdf         mov
            s.bx = (s.di) & 0xFFFF
            # 1010:0C60  c1eb08       shift rm16,imm8
            _r = cpu.shift(5, s.bx, 0x08, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:0C63  d1e3         shift
            _r = cpu.shift(4, s.bx, 1, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:0C65  8b87e400     mov
            _o = (s.bx + 0xE4) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:0C69  e92800       jmp
            cpu.instruction_count += 5
            bb = 13
        elif bb == 8:
            # 1010:0C6F  b80000       mov r16,imm16
            s.ax = 0x0
            # 1010:0C72  e91f00       jmp
            cpu.instruction_count += 2
            bb = 13
        elif bb == 9:
            # 1010:0C78  8bc6         mov
            s.ax = (s.si) & 0xFFFF
            # 1010:0C7A  250f00       and
            _a = s.ax
            _b = 0xF
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0C7D  3d0000       cmp
            _a = s.ax
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0C80  7503         jnz
            cpu.instruction_count += 4
            bb = 11 if cpu.condition(0x5) else 10
        elif bb == 10:
            # 1010:0C82  e90900       jmp
            cpu.instruction_count += 1
            bb = 12
        elif bb == 11:
            # 1010:0C85  b80028       mov r16,imm16
            s.ax = 0x2800
            # 1010:0C88  e90900       jmp
            cpu.instruction_count += 2
            bb = 13
        elif bb == 12:
            # 1010:0C8E  b80000       mov r16,imm16
            s.ax = 0x0
            # 1010:0C91  e90000       jmp
            cpu.instruction_count += 2
            bb = 13
        elif bb == 13:
            # 1010:0C94  5f           pop r16
            s.di = cpu.pop()
            # 1010:0C95  5e           pop r16
            s.si = cpu.pop()
            # 1010:0C96  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:0C97  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_0c26.owns_time = True

