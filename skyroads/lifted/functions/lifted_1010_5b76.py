"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5B76  (37 instructions, 14 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5B76)
SIGNATURE = bytes.fromhex('c8000000833eb60c00743d')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5B76, 1: 0x5B81, 2: 0x5B8E, 3: 0x5B95, 4: 0x5B9A, 5: 0x5B9D, 6: 0x5BA2, 7: 0x5BA6, 8: 0x5BAB, 9: 0x5BAE, 10: 0x5BB3, 11: 0x5BB6, 12: 0x5BBB, 13: 0x5BBE}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:5B8E": 2}


def lifted_1010_5b76(cpu, bb=0):
    """Lifted replacement for 1010:5B76."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5B76, SIGNATURE, 'lifted_1010_5b76')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5B76  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:5B7A  833eb60c00   grp1 rm16,imm8sx
            _o = 0x0CB6
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5B7F  743d         jz
            cpu.instruction_count += 3
            bb = 13 if cpu.condition(0x4) else 1
        elif bb == 1:
            # 1010:5B81  8b4e0a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5B84  49           dec r16
            _old = s.cx
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.cx = _r
            # 1010:5B85  8b5e06       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5B88  8b5604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5B8B  e8abff       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x5B39, 0x5B8E)
            bb = 2
        elif bb == 2:
            # 1010:5B8E  8b16b60c     mov
            _o = 0x0CB6
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5B92  83c20c       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0xC
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 2
            bb = 3
        elif bb == 3:
            # 1010:5B95  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5B96  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5B98  72fb         jb
            cpu.instruction_count += 3
            bb = 3 if cpu.condition(0x2) else 4
        elif bb == 4:
            # 1010:5B9A  b040         mov r8,imm8
            cpu.set_reg8(0, 0x40)
            # 1010:5B9C  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            cpu.instruction_count += 2
            bb = 5
        elif bb == 5:
            # 1010:5B9D  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5B9E  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5BA0  72fb         jb
            cpu.instruction_count += 3
            bb = 5 if cpu.condition(0x2) else 6
        elif bb == 6:
            # 1010:5BA2  8a4608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ss, _o))
            # 1010:5BA5  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            cpu.instruction_count += 2
            bb = 7
        elif bb == 7:
            # 1010:5BA6  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5BA7  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5BA9  72fb         jb
            cpu.instruction_count += 3
            bb = 7 if cpu.condition(0x2) else 8
        elif bb == 8:
            # 1010:5BAB  b014         mov r8,imm8
            cpu.set_reg8(0, 0x14)
            # 1010:5BAD  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            cpu.instruction_count += 2
            bb = 9
        elif bb == 9:
            # 1010:5BAE  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5BAF  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5BB1  72fb         jb
            cpu.instruction_count += 3
            bb = 9 if cpu.condition(0x2) else 10
        elif bb == 10:
            # 1010:5BB3  8ac1         mov
            cpu.set_reg8(0, cpu.get_reg8(1))
            # 1010:5BB5  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            cpu.instruction_count += 2
            bb = 11
        elif bb == 11:
            # 1010:5BB6  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5BB7  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5BB9  72fb         jb
            cpu.instruction_count += 3
            bb = 11 if cpu.condition(0x2) else 12
        elif bb == 12:
            # 1010:5BBB  8ac5         mov
            cpu.set_reg8(0, cpu.get_reg8(5))
            # 1010:5BBD  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            cpu.instruction_count += 2
            bb = 13
        elif bb == 13:
            # 1010:5BBE  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:5BBF  c3           ret
            cpu.instruction_count += 2
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5b76.owns_time = True

