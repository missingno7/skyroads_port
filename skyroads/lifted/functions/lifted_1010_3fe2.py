"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:3FE2  (45 instructions, 11 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x3FE2)
SIGNATURE = bytes.fromhex('c804000056578b7604e88825')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x3FE2, 1: 0x3FEE, 2: 0x3FF1, 3: 0x3FFA, 4: 0x400B, 5: 0x401F, 6: 0x4022, 7: 0x4032, 8: 0x4045, 9: 0x4048, 10: 0x404E}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:3FEE": 1, "1010:3FF1": 2, "1010:3FFA": 3, "1010:400B": 4, "1010:4032": 7, "1010:4045": 8}


def lifted_1010_3fe2(cpu, bb=0):
    """Lifted replacement for 1010:3FE2."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x3FE2, SIGNATURE, 'lifted_1010_3fe2')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:3FE2  c8040000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x4) & 0xFFFF
            # 1010:3FE6  56           push r16
            cpu.push(s.si)
            # 1010:3FE7  57           push r16
            cpu.push(s.di)
            # 1010:3FE8  8b7604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:3FEB  e88825       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x6576, 0x3FEE)
            bb = 1
        elif bb == 1:
            # 1010:3FEE  e88525       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x6576, 0x3FF1)
            bb = 2
        elif bb == 2:
            # 1010:3FF1  8b4606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:3FF4  894402       mov
            _o = (s.si + 0x2) & 0xFFFF
            mem.ww(s.ds, _o, s.ax)
            # 1010:3FF7  e89624       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x6490, 0x3FFA)
            bb = 3
        elif bb == 3:
            # 1010:3FFA  894404       mov
            _o = (s.si + 0x4) & 0xFFFF
            mem.ww(s.ds, _o, s.ax)
            # 1010:3FFD  b80300       mov r16,imm16
            s.ax = 0x3
            # 1010:4000  f76c04       grp3 rm16
            _o = (s.si + 0x4) & 0xFFFF
            _v = mem.rw(s.ds, _o)
            _result = cpu.sign16(s.ax) * cpu.sign16(_v)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = not (-32768 <= _result <= 32767)
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:4003  ba0000       mov r16,imm16
            s.dx = 0x0
            # 1010:4006  52           push r16
            cpu.push(s.dx)
            # 1010:4007  50           push r16
            cpu.push(s.ax)
            # 1010:4008  e8e9fe       call
            cpu.instruction_count += 7
            emulate_call(cpu, 0x1010, 0x3EF4, 0x400B)
            bb = 4
        elif bb == 4:
            # 1010:400B  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:400E  8904         mov
            _o = (s.si) & 0xFFFF
            mem.ww(s.ds, _o, s.ax)
            # 1010:4010  8946fe       mov
            _o = (s.bp - 0x2) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:4013  c746fc0000   mov rm16,imm16
            _o = (s.bp - 0x4) & 0xFFFF
            mem.ww(s.ss, _o, 0x0)
            # 1010:4018  833eaa4100   grp1 rm16,imm8sx
            _o = 0x41AA
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:401D  7403         jz
            cpu.instruction_count += 6
            bb = 6 if cpu.condition(0x4) else 5
        elif bb == 5:
            # 1010:401F  e92600       jmp
            cpu.instruction_count += 1
            bb = 9
        elif bb == 6:
            # 1010:4022  b80300       mov r16,imm16
            s.ax = 0x3
            # 1010:4025  f76c04       grp3 rm16
            _o = (s.si + 0x4) & 0xFFFF
            _v = mem.rw(s.ds, _o)
            _result = cpu.sign16(s.ax) * cpu.sign16(_v)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = not (-32768 <= _result <= 32767)
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:4028  50           push r16
            cpu.push(s.ax)
            # 1010:4029  ff76fe       push rm16
            _o = (s.bp - 0x2) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:402C  ff76fc       push rm16
            _o = (s.bp - 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:402F  e86325       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x6595, 0x4032)
            bb = 7
        elif bb == 7:
            # 1010:4032  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4035  8b4404       mov
            _o = (s.si + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:4038  d1e0         shift
            _r = cpu.shift(4, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:403A  50           push r16
            cpu.push(s.ax)
            # 1010:403B  b83caf       mov r16,imm16
            s.ax = 0xAF3C
            # 1010:403E  8cda         mov rm16,sreg
            s.dx = (s.ds) & 0xFFFF
            # 1010:4040  52           push r16
            cpu.push(s.dx)
            # 1010:4041  50           push r16
            cpu.push(s.ax)
            # 1010:4042  e85025       call
            cpu.instruction_count += 9
            emulate_call(cpu, 0x1010, 0x6595, 0x4045)
            bb = 8
        elif bb == 8:
            # 1010:4045  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 9
        elif bb == 9:
            # 1010:4048  8b4404       mov
            _o = (s.si + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:404B  e90000       jmp
            cpu.instruction_count += 2
            bb = 10
        elif bb == 10:
            # 1010:404E  5f           pop r16
            s.di = cpu.pop()
            # 1010:404F  5e           pop r16
            s.si = cpu.pop()
            # 1010:4050  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:4051  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_3fe2.owns_time = True

