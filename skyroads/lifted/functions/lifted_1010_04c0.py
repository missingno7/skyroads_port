"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:04C0  (53 instructions, 8 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x04C0)
SIGNATURE = bytes.fromhex('c800000056578b7608b980008bc62bd2')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x04C0, 1: 0x04DD, 2: 0x04E0, 3: 0x04F3, 4: 0x0500, 5: 0x050D, 6: 0x0529, 7: 0x052F}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:04F3": 3, "1010:0500": 4, "1010:050D": 5}


def lifted_1010_04c0(cpu, bb=0):
    """Lifted replacement for 1010:04C0."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x04C0, SIGNATURE, 'lifted_1010_04c0')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:04C0  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:04C4  56           push r16
            cpu.push(s.si)
            # 1010:04C5  57           push r16
            cpu.push(s.di)
            # 1010:04C6  8b7608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:04C9  b98000       mov r16,imm16
            s.cx = 0x80
            # 1010:04CC  8bc6         mov
            s.ax = (s.si) & 0xFFFF
            # 1010:04CE  2bd2         sub
            _a = s.dx
            _b = s.dx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:04D0  f7f1         grp3 rm16
            _v = s.cx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:04D2  05a1ff       add
            _a = s.ax
            _b = 0xFFA1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:04D5  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:04D7  81fe4201     grp1 rm16,imm16
            _a = s.si
            _b = 0x142
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:04DB  7203         jb
            cpu.instruction_count += 12
            bb = 2 if cpu.condition(0x2) else 1
        elif bb == 1:
            # 1010:04DD  e94900       jmp
            cpu.instruction_count += 1
            bb = 6
        elif bb == 2:
            # 1010:04E0  b80020       mov r16,imm16
            s.ax = 0x2000
            # 1010:04E3  ba0000       mov r16,imm16
            s.dx = 0x0
            # 1010:04E6  8b4e04       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:04E9  8b5e06       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:04EC  52           push r16
            cpu.push(s.dx)
            # 1010:04ED  50           push r16
            cpu.push(s.ax)
            # 1010:04EE  53           push r16
            cpu.push(s.bx)
            # 1010:04EF  51           push r16
            cpu.push(s.cx)
            # 1010:04F0  e89958       call
            cpu.instruction_count += 9
            emulate_call(cpu, 0x1010, 0x5D8C, 0x04F3)
            bb = 3
        elif bb == 3:
            # 1010:04F3  b90800       mov r16,imm16
            s.cx = 0x8
            # 1010:04F6  bb0000       mov r16,imm16
            s.bx = 0x0
            # 1010:04F9  53           push r16
            cpu.push(s.bx)
            # 1010:04FA  51           push r16
            cpu.push(s.cx)
            # 1010:04FB  52           push r16
            cpu.push(s.dx)
            # 1010:04FC  50           push r16
            cpu.push(s.ax)
            # 1010:04FD  e88c58       call
            cpu.instruction_count += 7
            emulate_call(cpu, 0x1010, 0x5D8C, 0x0500)
            bb = 4
        elif bb == 4:
            # 1010:0500  b90e00       mov r16,imm16
            s.cx = 0xE
            # 1010:0503  bb0000       mov r16,imm16
            s.bx = 0x0
            # 1010:0506  53           push r16
            cpu.push(s.bx)
            # 1010:0507  51           push r16
            cpu.push(s.cx)
            # 1010:0508  52           push r16
            cpu.push(s.dx)
            # 1010:0509  50           push r16
            cpu.push(s.ax)
            # 1010:050A  e83f58       call
            cpu.instruction_count += 7
            emulate_call(cpu, 0x1010, 0x5D4C, 0x050D)
            bb = 5
        elif bb == 5:
            # 1010:050D  b92c16       mov r16,imm16
            s.cx = 0x162C
            # 1010:0510  03c8         add
            _a = s.cx
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:0512  bb2e00       mov r16,imm16
            s.bx = 0x2E
            # 1010:0515  8bc6         mov
            s.ax = (s.si) & 0xFFFF
            # 1010:0517  2bd2         sub
            _a = s.dx
            _b = s.dx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:0519  f7f3         grp3 rm16
            _v = s.bx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:051B  d1e0         shift
            _r = cpu.shift(4, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:051D  03c8         add
            _a = s.cx
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:051F  8bd9         mov
            s.bx = (s.cx) & 0xFFFF
            # 1010:0521  8b07         mov
            _o = (s.bx) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:0523  e90900       jmp
            cpu.instruction_count += 11
            bb = 7
        elif bb == 6:
            # 1010:0529  b80000       mov r16,imm16
            s.ax = 0x0
            # 1010:052C  e90000       jmp
            cpu.instruction_count += 2
            bb = 7
        elif bb == 7:
            # 1010:052F  5f           pop r16
            s.di = cpu.pop()
            # 1010:0530  5e           pop r16
            s.si = cpu.pop()
            # 1010:0531  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:0532  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_04c0.owns_time = True

