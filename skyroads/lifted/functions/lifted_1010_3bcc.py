"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:3BCC  (25 instructions, 4 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x3BCC)
SIGNATURE = bytes.fromhex('601e2e8e1eca3ae460bbd20b3a077406')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x3BCC, 1: 0x3BD8, 2: 0x3BDC, 3: 0x3BE2}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_3bcc(cpu, bb=0):
    """Lifted replacement for 1010:3BCC."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x3BCC, SIGNATURE, 'lifted_1010_3bcc')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:3BCC  60           pusha
            _sp0 = s.sp
            cpu.push(s.ax)
            cpu.push(s.cx)
            cpu.push(s.dx)
            cpu.push(s.bx)
            cpu.push(_sp0)
            cpu.push(s.bp)
            cpu.push(s.si)
            cpu.push(s.di)
            # 1010:3BCD  1e           push ds
            cpu.push(s.ds)
            # 1010:3BCE  2e8e1eca3a   mov sreg,rm16
            _o = 0x3ACA
            cpu.set_sreg(3, mem.rw(s.cs, _o))
            # 1010:3BD3  e460         in al,imm8
            _val = cpu.port_reader(cpu, (0x60) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:3BD5  bbd20b       mov r16,imm16
            s.bx = 0xBD2
            cpu.instruction_count += 5
            bb = 1
        elif bb == 1:
            # 1010:3BD8  3a07         cmp
            _o = (s.bx) & 0xFFFF
            _a = cpu.get_reg8(0)
            _b = mem.rb(s.ds, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3BDA  7406         jz
            cpu.instruction_count += 2
            bb = 3 if cpu.condition(0x4) else 2
        elif bb == 2:
            # 1010:3BDC  43           inc r16
            _old = s.bx
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.bx = _r
            # 1010:3BDD  f6077f       grp3 rm8
            _o = (s.bx) & 0xFFFF
            _v = mem.rb(s.ds, _o)
            cpu.set_logic_flags(_v & 0x7F, 8)
            # 1010:3BE0  75f6         jnz
            cpu.instruction_count += 3
            bb = 1 if cpu.condition(0x5) else 3
        elif bb == 3:
            # 1010:3BE2  80277f       grp1 rm8,imm8
            _o = (s.bx) & 0xFFFF
            _a = mem.rb(s.ds, _o)
            _b = 0x7F
            _r = _a & _b
            cpu.set_logic_flags(_r, 8)
            mem.wb(s.ds, _o, _r & 0xFF)
            # 1010:3BE5  2480         and
            _a = cpu.get_reg8(0)
            _b = 0x80
            _r = _a & _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:3BE7  3480         xor
            _a = cpu.get_reg8(0)
            _b = 0x80
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:3BE9  0807         or
            _o = (s.bx) & 0xFFFF
            _a = mem.rb(s.ds, _o)
            _b = cpu.get_reg8(0)
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            mem.wb(s.ds, _o, _r & 0xFF)
            # 1010:3BEB  e461         in al,imm8
            _val = cpu.port_reader(cpu, (0x61) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:3BED  8ae0         mov
            cpu.set_reg8(4, cpu.get_reg8(0))
            # 1010:3BEF  0c80         or
            _a = cpu.get_reg8(0)
            _b = 0x80
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:3BF1  e661         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x61) & 0xFFFF, _val, 8)
            # 1010:3BF3  8ac4         mov
            cpu.set_reg8(0, cpu.get_reg8(4))
            # 1010:3BF5  e661         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x61) & 0xFFFF, _val, 8)
            # 1010:3BF7  b020         mov r8,imm8
            cpu.set_reg8(0, 0x20)
            # 1010:3BF9  e620         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x20) & 0xFFFF, _val, 8)
            # 1010:3BFB  1f           pop ds
            cpu.set_sreg(3, cpu.pop())
            # 1010:3BFC  61           popa
            s.di = cpu.pop()
            s.si = cpu.pop()
            s.bp = cpu.pop()
            cpu.pop()  # discard the saved SP
            s.bx = cpu.pop()
            s.dx = cpu.pop()
            s.cx = cpu.pop()
            s.ax = cpu.pop()
            # 1010:3BFD  cf           iret
            cpu.instruction_count += 15
            s.ip = cpu.pop()
            s.cs = cpu.pop()
            s.flags = cpu.pop() | 0x0002
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_3bcc.owns_time = True

