"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5A98  (72 instructions, 12 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5A98)
SIGNATURE = bytes.fromhex('fa8b16b60c83c206b001ee32c0e643e4')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5A98, 1: 0x5AB3, 2: 0x5ACA, 3: 0x5AE4, 4: 0x5AF0, 5: 0x5B08, 6: 0x5B15, 7: 0x5B1C, 8: 0x5B28, 9: 0x5B2F, 10: 0x5B34, 11: 0x5B37}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_5a98(cpu, bb=0):
    """Lifted replacement for 1010:5A98."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5A98, SIGNATURE, 'lifted_1010_5a98')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5A98  fa           cli
            cpu.set_flag(IF, False)
            # 1010:5A99  8b16b60c     mov
            _o = 0x0CB6
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5A9D  83c206       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5AA0  b001         mov r8,imm8
            cpu.set_reg8(0, 0x1)
            # 1010:5AA2  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5AA3  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AA5  e643         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x43) & 0xFFFF, _val, 8)
            # 1010:5AA7  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5AA9  8ad0         mov
            cpu.set_reg8(2, cpu.get_reg8(0))
            # 1010:5AAB  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5AAD  8af0         mov
            cpu.set_reg8(6, cpu.get_reg8(0))
            # 1010:5AAF  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AB1  8bca         mov
            s.cx = (s.dx) & 0xFFFF
            cpu.instruction_count += 13
            bb = 1
        elif bb == 1:
            # 1010:5AB3  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AB5  e643         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x43) & 0xFFFF, _val, 8)
            # 1010:5AB7  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5AB9  8ad0         mov
            cpu.set_reg8(2, cpu.get_reg8(0))
            # 1010:5ABB  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5ABD  8af0         mov
            cpu.set_reg8(6, cpu.get_reg8(0))
            # 1010:5ABF  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AC1  2bd1         sub
            _a = s.dx
            _b = s.cx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5AC3  f7da         grp3 rm16
            _v = s.dx
            cpu.set_sub_flags(0, _v, -_v, 16)
            s.dx = ((-_v) & 0xFFFF) & 0xFFFF
            # 1010:5AC5  83fa08       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0x8
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5AC8  72e9         jb
            cpu.instruction_count += 11
            bb = 1 if cpu.condition(0x2) else 2
        elif bb == 2:
            # 1010:5ACA  8b16b60c     mov
            _o = 0x0CB6
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5ACE  83c206       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5AD1  b000         mov r8,imm8
            cpu.set_reg8(0, 0x0)
            # 1010:5AD3  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5AD4  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AD6  e643         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x43) & 0xFFFF, _val, 8)
            # 1010:5AD8  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5ADA  8ad0         mov
            cpu.set_reg8(2, cpu.get_reg8(0))
            # 1010:5ADC  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5ADE  8af0         mov
            cpu.set_reg8(6, cpu.get_reg8(0))
            # 1010:5AE0  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AE2  8bca         mov
            s.cx = (s.dx) & 0xFFFF
            cpu.instruction_count += 12
            bb = 3
        elif bb == 3:
            # 1010:5AE4  8b16b60c     mov
            _o = 0x0CB6
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5AE8  83c20e       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0xE
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5AEB  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5AEC  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5AEE  722c         jb
            cpu.instruction_count += 5
            bb = 7 if cpu.condition(0x2) else 4
        elif bb == 4:
            # 1010:5AF0  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AF2  e643         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x43) & 0xFFFF, _val, 8)
            # 1010:5AF4  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5AF6  8ad0         mov
            cpu.set_reg8(2, cpu.get_reg8(0))
            # 1010:5AF8  e440         in al,imm8
            _val = cpu.port_reader(cpu, (0x40) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5AFA  8af0         mov
            cpu.set_reg8(6, cpu.get_reg8(0))
            # 1010:5AFC  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5AFE  2bd1         sub
            _a = s.dx
            _b = s.cx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5B00  f7da         grp3 rm16
            _v = s.dx
            cpu.set_sub_flags(0, _v, -_v, 16)
            s.dx = ((-_v) & 0xFFFF) & 0xFFFF
            # 1010:5B02  81faf000     grp1 rm16,imm16
            _a = s.dx
            _b = 0xF0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5B06  72dc         jb
            cpu.instruction_count += 11
            bb = 3 if cpu.condition(0x2) else 5
        elif bb == 5:
            # 1010:5B08  8306b60c10   grp1 rm16,imm8sx
            _o = 0x0CB6
            _a = mem.rw(s.ds, _o)
            _b = 0x10
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ds, _o, _r & 0xFFFF)
            # 1010:5B0D  813eb60c6002 grp1 rm16,imm16
            _o = 0x0CB6
            _a = mem.rw(s.ds, _o)
            _b = 0x260
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5B13  7683         jbe
            cpu.instruction_count += 3
            bb = 0 if cpu.condition(0x6) else 6
        elif bb == 6:
            # 1010:5B15  33c0         xor
            _a = s.ax
            _b = s.ax
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:5B17  a3b60c       mov moffs
            mem.ww(s.ds, 0x0CB6, s.ax)
            # 1010:5B1A  eb1b         jmp short
            cpu.instruction_count += 3
            bb = 11
        elif bb == 7:
            # 1010:5B1C  8b16b60c     mov
            _o = 0x0CB6
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5B20  83c20a       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0xA
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5B23  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5B24  3caa         cmp
            _a = cpu.get_reg8(0)
            _b = 0xAA
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:5B26  75c8         jnz
            cpu.instruction_count += 5
            bb = 4 if cpu.condition(0x5) else 8
        elif bb == 8:
            # 1010:5B28  8b16b60c     mov
            _o = 0x0CB6
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5B2C  83c20c       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0xC
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 2
            bb = 9
        elif bb == 9:
            # 1010:5B2F  ec           in al,dx
            _val = cpu.port_reader(cpu, (s.dx) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:5B30  d0e0         shift
            _r = cpu.shift(4, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:5B32  72fb         jb
            cpu.instruction_count += 3
            bb = 9 if cpu.condition(0x2) else 10
        elif bb == 10:
            # 1010:5B34  b0d1         mov r8,imm8
            cpu.set_reg8(0, 0xD1)
            # 1010:5B36  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            cpu.instruction_count += 2
            bb = 11
        elif bb == 11:
            # 1010:5B37  fb           sti
            cpu.set_flag(IF, True)
            # 1010:5B38  c3           ret
            cpu.instruction_count += 2
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5a98.owns_time = True

