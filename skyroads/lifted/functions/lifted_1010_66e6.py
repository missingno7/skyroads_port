"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:66E6  (54 instructions, 20 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x66E6)
SIGNATURE = bytes.fromhex('c80000005657c47e04017e08e89bfd')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x66E6, 1: 0x66F5, 2: 0x66FC, 3: 0x670E, 4: 0x6712, 5: 0x6717, 6: 0x671A, 7: 0x671E, 8: 0x6723, 9: 0x672D, 10: 0x673C, 11: 0x6740, 12: 0x6744, 13: 0x6747, 14: 0x674B, 15: 0x6750, 16: 0x6755, 17: 0x675A, 18: 0x6760, 19: 0x6763}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:66F5": 1, "1010:66FC": 2, "1010:670E": 3, "1010:671A": 6, "1010:6723": 8, "1010:672D": 9, "1010:6747": 13, "1010:6750": 15, "1010:675A": 17, "1010:6763": 19}


def lifted_1010_66e6(cpu, bb=0):
    """Lifted replacement for 1010:66E6."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x66E6, SIGNATURE, 'lifted_1010_66e6')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:66E6  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:66EA  56           push r16
            cpu.push(s.si)
            # 1010:66EB  57           push r16
            cpu.push(s.di)
            # 1010:66EC  c47e04       les
            _o = (s.bp + 0x4) & 0xFFFF
            _off = mem.rw(s.ss, _o)
            _seg = mem.rw(s.ss, (_o + 2) & 0xFFFF)
            s.di = _off
            s.es = _seg
            # 1010:66EF  017e08       add
            _o = (s.bp + 0x8) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = s.di
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ss, _o, _r & 0xFFFF)
            # 1010:66F2  e89bfd       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x6490, 0x66F5)
            bb = 1
        elif bb == 1:
            # 1010:66F5  2ea22967     mov moffs
            mem.wb(s.cs, 0x6729, s.ax & 0xFF)
            # 1010:66F9  e894fd       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x6490, 0x66FC)
            bb = 2
        elif bb == 2:
            # 1010:66FC  2ea21f67     mov moffs
            mem.wb(s.cs, 0x671F, s.ax & 0xFF)
            # 1010:6700  8bc8         mov
            s.cx = (s.ax) & 0xFFFF
            # 1010:6702  b80100       mov r16,imm16
            s.ax = 0x1
            # 1010:6705  d3e0         shift
            _r = cpu.shift(4, s.ax, s.cx & 0xFF, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:6707  2ea35167     mov moffs
            mem.ww(s.cs, 0x6751, s.ax)
            # 1010:670B  e882fd       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x6490, 0x670E)
            bb = 3
        elif bb == 3:
            # 1010:670E  2ea24c67     mov moffs
            mem.wb(s.cs, 0x674C, s.ax & 0xFF)
            cpu.instruction_count += 1
            bb = 4
        elif bb == 4:
            # 1010:6712  3b7e08       cmp
            _o = (s.bp + 0x8) & 0xFFFF
            _a = s.di
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:6715  7349         jnb
            cpu.instruction_count += 2
            bb = 18 if cpu.condition(0x3) else 5
        elif bb == 5:
            # 1010:6717  e891fd       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x64AB, 0x671A)
            bb = 6
        elif bb == 6:
            # 1010:671A  0bc0         or
            _a = s.ax
            _b = s.ax
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:671C  7526         jnz
            cpu.instruction_count += 2
            bb = 12 if cpu.condition(0x5) else 7
        elif bb == 7:
            # 1010:671E  6a0a         push imm8
            _pi = mem.rb(s.cs, 0x671F)
            cpu.push((_pi | 0xFF00) if _pi & 0x80 else _pi)
            # 1010:6720  e8dcfd       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x64FF, 0x6723)
            bb = 8
        elif bb == 8:
            # 1010:6723  8d75fe       lea
            _o = (s.di - 0x2) & 0xFFFF
            s.si = _o
            # 1010:6726  2bf0         sub
            _a = s.si
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:6728  6a05         push imm8
            _pi = mem.rb(s.cs, 0x6729)
            cpu.push((_pi | 0xFF00) if _pi & 0x80 else _pi)
            # 1010:672A  e8d2fd       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x64FF, 0x672D)
            bb = 9
        elif bb == 9:
            # 1010:672D  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:6730  8bc8         mov
            s.cx = (s.ax) & 0xFFFF
            # 1010:6732  41           inc r16
            _old = s.cx
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.cx = _r
            # 1010:6733  8b5608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:6736  2bd7         sub
            _a = s.dx
            _b = s.di
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:6738  3bca         cmp
            _a = s.cx
            _b = s.dx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:673A  7324         jnb
            cpu.instruction_count += 7
            bb = 18 if cpu.condition(0x3) else 10
        elif bb == 10:
            # 1010:673C  26a4         movs/cmps
            cpu.string_op(0xA4, None, 'es')
            # 1010:673E  e2fc         loop
            cpu.instruction_count += 2
            s.cx = (s.cx - 1) & 0xFFFF
            bb = 10 if (s.cx != 0) else 11
        elif bb == 11:
            # 1010:6740  26a4         movs/cmps
            cpu.string_op(0xA4, None, 'es')
            # 1010:6742  ebce         jmp short
            cpu.instruction_count += 2
            bb = 4
        elif bb == 12:
            # 1010:6744  e864fd       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x64AB, 0x6747)
            bb = 13
        elif bb == 13:
            # 1010:6747  0bc0         or
            _a = s.ax
            _b = s.ax
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:6749  750a         jnz
            cpu.instruction_count += 2
            bb = 16 if cpu.condition(0x5) else 14
        elif bb == 14:
            # 1010:674B  6a0b         push imm8
            _pi = mem.rb(s.cs, 0x674C)
            cpu.push((_pi | 0xFF00) if _pi & 0x80 else _pi)
            # 1010:674D  e8affd       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x64FF, 0x6750)
            bb = 15
        elif bb == 15:
            # 1010:6750  050004       add
            _a = s.ax
            _b = mem.rw(s.cs, 0x6751)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:6753  ebce         jmp short
            cpu.instruction_count += 2
            bb = 8
        elif bb == 16:
            # 1010:6755  6a08         push imm8
            cpu.push(0x0008)
            # 1010:6757  e8a5fd       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x64FF, 0x675A)
            bb = 17
        elif bb == 17:
            # 1010:675A  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:675D  aa           stos/lods/scas
            cpu.string_op(0xAA, None, None)
            # 1010:675E  ebb2         jmp short
            cpu.instruction_count += 3
            bb = 4
        elif bb == 18:
            # 1010:6760  e816ff       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x6679, 0x6763)
            bb = 19
        elif bb == 19:
            # 1010:6763  5f           pop r16
            s.di = cpu.pop()
            # 1010:6764  5e           pop r16
            s.si = cpu.pop()
            # 1010:6765  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:6766  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_66e6.owns_time = True

