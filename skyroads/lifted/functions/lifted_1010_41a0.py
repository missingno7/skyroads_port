"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:41A0  (43 instructions, 8 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x41A0)
SIGNATURE = bytes.fromhex('c800000056571ea014962ea2e9418a26')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x41A0, 1: 0x41DC, 2: 0x41DF, 3: 0x41E7, 4: 0x41EC, 5: 0x41F0, 6: 0x41F1, 7: 0x41F3}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_41a0(cpu, bb=0):
    """Lifted replacement for 1010:41A0."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x41A0, SIGNATURE, 'lifted_1010_41a0')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:41A0  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:41A4  56           push r16
            cpu.push(s.si)
            # 1010:41A5  57           push r16
            cpu.push(s.di)
            # 1010:41A6  1e           push ds
            cpu.push(s.ds)
            # 1010:41A7  a01496       mov moffs
            s.ax = (s.ax & 0xFF00) | mem.rb(s.ds, 0x9614)
            # 1010:41AA  2ea2e941     mov moffs
            mem.wb(s.cs, 0x41E9, s.ax & 0xFF)
            # 1010:41AE  8a263aaf     mov
            _o = 0xAF3A
            cpu.set_reg8(4, mem.rb(s.ds, _o))
            # 1010:41B2  8b161245     mov
            _o = 0x4512
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:41B6  8b1e1445     mov
            _o = 0x4514
            s.bx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:41BA  8e061c96     mov sreg,rm16
            _o = 0x961C
            cpu.set_sreg(0, mem.rw(s.ds, _o))
            # 1010:41BE  8b7e04       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.di = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:41C1  8bf7         mov
            s.si = (s.di) & 0xFFFF
            # 1010:41C3  8eda         mov sreg,rm16
            cpu.set_sreg(3, s.dx)
            # 1010:41C5  8b4e08       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:41C8  f3a4         movs/cmps
            cpu.string_op(0xA4, 0xF3, None)
            # 1010:41CA  8b7606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:41CD  368b0e1296   mov
            _o = 0x9612
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:41D2  2b4e08       sub
            _o = (s.bp + 0x8) & 0xFFFF
            _a = s.cx
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:41D5  2b4e0a       sub
            _o = (s.bp + 0xA) & 0xFFFF
            _a = s.cx
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:41D8  8edb         mov sreg,rm16
            cpu.set_sreg(3, s.bx)
            # 1010:41DA  eb0b         jmp short
            cpu.instruction_count += 21
            bb = 3
        elif bb == 1:
            # 1010:41DC  47           inc r16
            _old = s.di
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.di = _r
            # 1010:41DD  eb12         jmp short
            cpu.instruction_count += 2
            bb = 6
        elif bb == 2:
            # 1010:41DF  8eda         mov sreg,rm16
            cpu.set_sreg(3, s.dx)
            # 1010:41E1  8a05         mov
            _o = (s.di) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ds, _o))
            # 1010:41E3  8edb         mov sreg,rm16
            cpu.set_sreg(3, s.bx)
            # 1010:41E5  eb09         jmp short
            cpu.instruction_count += 4
            bb = 5
        elif bb == 3:
            # 1010:41E7  ac           stos/lods/scas
            cpu.string_op(0xAC, None, None)
            # 1010:41E8  3c01         cmp
            _a = cpu.get_reg8(0)
            _b = mem.rb(s.cs, 0x41E9)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:41EA  72f0         jb
            cpu.instruction_count += 3
            bb = 1 if cpu.condition(0x2) else 4
        elif bb == 4:
            # 1010:41EC  3ac4         cmp
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(4)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:41EE  72ef         jb
            cpu.instruction_count += 2
            bb = 2 if cpu.condition(0x2) else 5
        elif bb == 5:
            # 1010:41F0  aa           stos/lods/scas
            cpu.string_op(0xAA, None, None)
            cpu.instruction_count += 1
            bb = 6
        elif bb == 6:
            # 1010:41F1  e2f4         loop
            cpu.instruction_count += 1
            s.cx = (s.cx - 1) & 0xFFFF
            bb = 3 if (s.cx != 0) else 7
        elif bb == 7:
            # 1010:41F3  8eda         mov sreg,rm16
            cpu.set_sreg(3, s.dx)
            # 1010:41F5  8b4e0a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:41F8  8bf7         mov
            s.si = (s.di) & 0xFFFF
            # 1010:41FA  f3a4         movs/cmps
            cpu.string_op(0xA4, 0xF3, None)
            # 1010:41FC  1f           pop ds
            cpu.set_sreg(3, cpu.pop())
            # 1010:41FD  5f           pop r16
            s.di = cpu.pop()
            # 1010:41FE  5e           pop r16
            s.si = cpu.pop()
            # 1010:41FF  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:4200  c3           ret
            cpu.instruction_count += 9
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_41a0.owns_time = True

