"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:1114  (52 instructions, 11 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x1114)
SIGNATURE = bytes.fromhex('c80400005657be0000e90300')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x1114, 1: 0x1120, 2: 0x1123, 3: 0x112A, 4: 0x112D, 5: 0x1133, 6: 0x1136, 7: 0x113B, 8: 0x113E, 9: 0x117A, 10: 0x118D}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:117A": 9}


def lifted_1010_1114(cpu, bb=0):
    """Lifted replacement for 1010:1114."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x1114, SIGNATURE, 'lifted_1010_1114')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:1114  c8040000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x4) & 0xFFFF
            # 1010:1118  56           push r16
            cpu.push(s.si)
            # 1010:1119  57           push r16
            cpu.push(s.di)
            # 1010:111A  be0000       mov r16,imm16
            s.si = 0x0
            # 1010:111D  e90300       jmp
            cpu.instruction_count += 5
            bb = 2
        elif bb == 1:
            # 1010:1120  83c601       grp1 rm16,imm8sx
            _a = s.si
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 2
        elif bb == 2:
            # 1010:1123  8b460a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:1126  3bf0         cmp
            _a = s.si
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:1128  7203         jb
            cpu.instruction_count += 3
            bb = 4 if cpu.condition(0x2) else 3
        elif bb == 3:
            # 1010:112A  e96000       jmp
            cpu.instruction_count += 1
            bb = 10
        elif bb == 4:
            # 1010:112D  837e0800     grp1 rm16,imm8sx
            _o = (s.bp + 0x8) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:1131  7403         jz
            cpu.instruction_count += 2
            bb = 6 if cpu.condition(0x4) else 5
        elif bb == 5:
            # 1010:1133  e90800       jmp
            cpu.instruction_count += 1
            bb = 8
        elif bb == 6:
            # 1010:1136  83fe00       grp1 rm16,imm8sx
            _a = s.si
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:1139  7403         jz
            cpu.instruction_count += 2
            bb = 8 if cpu.condition(0x4) else 7
        elif bb == 7:
            # 1010:113B  e94f00       jmp
            cpu.instruction_count += 1
            bb = 10
        elif bb == 8:
            # 1010:113E  8bde         mov
            s.bx = (s.si) & 0xFFFF
            # 1010:1140  d1e3         shift
            _r = cpu.shift(4, s.bx, 1, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:1142  8b4608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:1145  2bd2         sub
            _a = s.dx
            _b = s.dx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:1147  f7b73803     grp3 rm16
            _o = (s.bx + 0x338) & 0xFFFF
            _v = mem.rw(s.ds, _o)
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:114B  b90a00       mov r16,imm16
            s.cx = 0xA
            # 1010:114E  2bd2         sub
            _a = s.dx
            _b = s.dx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:1150  f7f1         grp3 rm16
            _v = s.cx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:1152  8bfa         mov
            s.di = (s.dx) & 0xFFFF
            # 1010:1154  b81400       mov r16,imm16
            s.ax = 0x14
            # 1010:1157  f7ef         grp3 rm16
            _v = s.di
            _result = cpu.sign16(s.ax) * cpu.sign16(_v)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = not (-32768 <= _result <= 32767)
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:1159  b96c01       mov r16,imm16
            s.cx = 0x16C
            # 1010:115C  03c8         add
            _a = s.cx
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:115E  51           push r16
            cpu.push(s.cx)
            # 1010:115F  6a05         push imm8
            cpu.push(0x0005)
            # 1010:1161  6a04         push imm8
            cpu.push(0x0004)
            # 1010:1163  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:1166  8b460a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:1169  2bc6         sub
            _a = s.ax
            _b = s.si
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:116B  05ffff       add
            _a = s.ax
            _b = 0xFFFF
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:116E  b90500       mov r16,imm16
            s.cx = 0x5
            # 1010:1171  f7e9         grp3 rm16
            _v = s.cx
            _result = cpu.sign16(s.ax) * cpu.sign16(_v)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = not (-32768 <= _result <= 32767)
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:1173  034604       add
            _o = (s.bp + 0x4) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:1176  50           push r16
            cpu.push(s.ax)
            # 1010:1177  e8f9fe       call
            cpu.instruction_count += 25
            emulate_call(cpu, 0x1010, 0x1073, 0x117A)
            bb = 9
        elif bb == 9:
            # 1010:117A  83c40a       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0xA
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:117D  8bde         mov
            s.bx = (s.si) & 0xFFFF
            # 1010:117F  d1e3         shift
            _r = cpu.shift(4, s.bx, 1, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:1181  8bc7         mov
            s.ax = (s.di) & 0xFFFF
            # 1010:1183  f7af3803     grp3 rm16
            _o = (s.bx + 0x338) & 0xFFFF
            _v = mem.rw(s.ds, _o)
            _result = cpu.sign16(s.ax) * cpu.sign16(_v)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = not (-32768 <= _result <= 32767)
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:1187  294608       sub
            _o = (s.bp + 0x8) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            mem.ww(s.ss, _o, _r & 0xFFFF)
            # 1010:118A  e993ff       jmp
            cpu.instruction_count += 7
            bb = 1
        elif bb == 10:
            # 1010:118D  5f           pop r16
            s.di = cpu.pop()
            # 1010:118E  5e           pop r16
            s.si = cpu.pop()
            # 1010:118F  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:1190  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_1114.owns_time = True

