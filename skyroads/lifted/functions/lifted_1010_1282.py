"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:1282  (49 instructions, 20 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x1282)
SIGNATURE = bytes.fromhex('c806000056578b7604e90300')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x1282, 1: 0x128E, 2: 0x1291, 3: 0x129B, 4: 0x129E, 5: 0x12A4, 6: 0x12A7, 7: 0x12B1, 8: 0x12B4, 9: 0x12B9, 10: 0x12C7, 11: 0x12CA, 12: 0x12D2, 13: 0x12D8, 14: 0x12E0, 15: 0x12E3, 16: 0x12EB, 17: 0x12EE, 18: 0x12F1, 19: 0x12F4}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:12B9": 9, "1010:12D2": 12, "1010:12EB": 16}


def lifted_1010_1282(cpu, bb=0):
    """Lifted replacement for 1010:1282."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x1282, SIGNATURE, 'lifted_1010_1282')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:1282  c8060000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x6) & 0xFFFF
            # 1010:1286  56           push r16
            cpu.push(s.si)
            # 1010:1287  57           push r16
            cpu.push(s.di)
            # 1010:1288  8b7604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:128B  e90300       jmp
            cpu.instruction_count += 5
            bb = 2
        elif bb == 1:
            # 1010:128E  83c601       grp1 rm16,imm8sx
            _a = s.si
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 2
        elif bb == 2:
            # 1010:1291  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:1294  034608       add
            _o = (s.bp + 0x8) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:1297  3bf0         cmp
            _a = s.si
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:1299  7203         jb
            cpu.instruction_count += 4
            bb = 4 if cpu.condition(0x2) else 3
        elif bb == 3:
            # 1010:129B  e95600       jmp
            cpu.instruction_count += 1
            bb = 19
        elif bb == 4:
            # 1010:129E  8b7e06       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.di = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:12A1  e90300       jmp
            cpu.instruction_count += 2
            bb = 6
        elif bb == 5:
            # 1010:12A4  83c701       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 6
        elif bb == 6:
            # 1010:12A7  8b4606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:12AA  03460a       add
            _o = (s.bp + 0xA) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:12AD  3bf8         cmp
            _a = s.di
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:12AF  7203         jb
            cpu.instruction_count += 4
            bb = 8 if cpu.condition(0x2) else 7
        elif bb == 7:
            # 1010:12B1  e93d00       jmp
            cpu.instruction_count += 1
            bb = 18
        elif bb == 8:
            # 1010:12B4  57           push r16
            cpu.push(s.di)
            # 1010:12B5  56           push r16
            cpu.push(s.si)
            # 1010:12B6  e8d8fe       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x1191, 0x12B9)
            bb = 9
        elif bb == 9:
            # 1010:12B9  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:12BC  8946fa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:12BF  8b460c       mov
            _o = (s.bp + 0xC) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:12C2  3946fa       cmp
            _o = (s.bp - 0x6) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:12C5  7403         jz
            cpu.instruction_count += 5
            bb = 11 if cpu.condition(0x4) else 10
        elif bb == 10:
            # 1010:12C7  e90e00       jmp
            cpu.instruction_count += 1
            bb = 13
        elif bb == 11:
            # 1010:12CA  ff760e       push rm16
            _o = (s.bp + 0xE) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:12CD  57           push r16
            cpu.push(s.di)
            # 1010:12CE  56           push r16
            cpu.push(s.si)
            # 1010:12CF  e801ff       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x11D3, 0x12D2)
            bb = 12
        elif bb == 12:
            # 1010:12D2  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:12D5  e91600       jmp
            cpu.instruction_count += 2
            bb = 17
        elif bb == 13:
            # 1010:12D8  8b460e       mov
            _o = (s.bp + 0xE) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:12DB  3946fa       cmp
            _o = (s.bp - 0x6) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:12DE  7403         jz
            cpu.instruction_count += 3
            bb = 15 if cpu.condition(0x4) else 14
        elif bb == 14:
            # 1010:12E0  e90b00       jmp
            cpu.instruction_count += 1
            bb = 17
        elif bb == 15:
            # 1010:12E3  ff760c       push rm16
            _o = (s.bp + 0xC) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:12E6  57           push r16
            cpu.push(s.di)
            # 1010:12E7  56           push r16
            cpu.push(s.si)
            # 1010:12E8  e8e8fe       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x11D3, 0x12EB)
            bb = 16
        elif bb == 16:
            # 1010:12EB  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 17
        elif bb == 17:
            # 1010:12EE  e9b3ff       jmp
            cpu.instruction_count += 1
            bb = 5
        elif bb == 18:
            # 1010:12F1  e99aff       jmp
            cpu.instruction_count += 1
            bb = 1
        elif bb == 19:
            # 1010:12F4  5f           pop r16
            s.di = cpu.pop()
            # 1010:12F5  5e           pop r16
            s.si = cpu.pop()
            # 1010:12F6  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:12F7  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_1282.owns_time = True

