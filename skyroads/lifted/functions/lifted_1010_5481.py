"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5481  (58 instructions, 9 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5481)
SIGNATURE = bytes.fromhex('c80800005657ff7604e8ea07')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5481, 1: 0x548D, 2: 0x54AA, 3: 0x54BD, 4: 0x54CA, 5: 0x54DD, 6: 0x54EA, 7: 0x54FA, 8: 0x5501}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:548D": 1, "1010:54AA": 2, "1010:54BD": 3, "1010:54CA": 4, "1010:54DD": 5, "1010:54EA": 6, "1010:54FA": 7, "1010:5501": 8}


def lifted_1010_5481(cpu, bb=0):
    """Lifted replacement for 1010:5481."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5481, SIGNATURE, 'lifted_1010_5481')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5481  c8080000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x8) & 0xFFFF
            # 1010:5485  56           push r16
            cpu.push(s.si)
            # 1010:5486  57           push r16
            cpu.push(s.di)
            # 1010:5487  ff7604       push rm16
            _o = (s.bp + 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:548A  e8ea07       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x5C77, 0x548D)
            bb = 1
        elif bb == 1:
            # 1010:548D  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:5490  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:5492  8b4608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5495  d1e0         shift
            _r = cpu.shift(4, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:5497  ba0000       mov r16,imm16
            s.dx = 0x0
            # 1010:549A  8946f8       mov
            _o = (s.bp - 0x8) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:549D  8956fa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            mem.ww(s.ss, _o, s.dx)
            # 1010:54A0  6a02         push imm8
            cpu.push(0x0002)
            # 1010:54A2  6a00         push imm8
            cpu.push(0x0000)
            # 1010:54A4  6a00         push imm8
            cpu.push(0x0000)
            # 1010:54A6  56           push r16
            cpu.push(s.si)
            # 1010:54A7  e8fc07       call
            cpu.instruction_count += 12
            emulate_call(cpu, 0x1010, 0x5CA6, 0x54AA)
            bb = 2
        elif bb == 2:
            # 1010:54AA  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:54AD  8b4ef8       mov
            _o = (s.bp - 0x8) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:54B0  8b5efa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:54B3  2bc1         sub
            _a = s.ax
            _b = s.cx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:54B5  1bd3         sbb
            _a = s.dx
            _b = s.bx
            _c = 1 if s.flags & CF else 0
            _r = _a - _b - _c
            cpu.set_sub_flags(_a, _b, _r, 16, _c)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:54B7  8bf8         mov
            s.di = (s.ax) & 0xFFFF
            # 1010:54B9  56           push r16
            cpu.push(s.si)
            # 1010:54BA  e8310c       call
            cpu.instruction_count += 8
            emulate_call(cpu, 0x1010, 0x60EE, 0x54BD)
            bb = 3
        elif bb == 3:
            # 1010:54BD  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:54C0  8bc7         mov
            s.ax = (s.di) & 0xFFFF
            # 1010:54C2  ba0000       mov r16,imm16
            s.dx = 0x0
            # 1010:54C5  52           push r16
            cpu.push(s.dx)
            # 1010:54C6  50           push r16
            cpu.push(s.ax)
            # 1010:54C7  e82aea       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x3EF4, 0x54CA)
            bb = 4
        elif bb == 4:
            # 1010:54CA  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:54CD  8b5e0a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:54D0  894702       mov
            _o = (s.bx + 0x2) & 0xFFFF
            mem.ww(s.ds, _o, s.ax)
            # 1010:54D3  8b5e0a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:54D6  c7070000     mov rm16,imm16
            _o = (s.bx) & 0xFFFF
            mem.ww(s.ds, _o, 0x0)
            # 1010:54DA  e88cab       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x0069, 0x54DD)
            bb = 5
        elif bb == 5:
            # 1010:54DD  8b4608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:54E0  d1e0         shift
            _r = cpu.shift(4, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:54E2  50           push r16
            cpu.push(s.ax)
            # 1010:54E3  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:54E6  56           push r16
            cpu.push(s.si)
            # 1010:54E7  e8930a       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x5F7D, 0x54EA)
            bb = 6
        elif bb == 6:
            # 1010:54EA  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:54ED  57           push r16
            cpu.push(s.di)
            # 1010:54EE  8b5e0a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:54F1  ff7702       push rm16
            _o = (s.bx + 0x2) & 0xFFFF
            cpu.push(mem.rw(s.ds, _o))
            # 1010:54F4  ff37         push rm16
            _o = (s.bx) & 0xFFFF
            cpu.push(mem.rw(s.ds, _o))
            # 1010:54F6  56           push r16
            cpu.push(s.si)
            # 1010:54F7  e8bb0a       call
            cpu.instruction_count += 7
            emulate_call(cpu, 0x1010, 0x5FB5, 0x54FA)
            bb = 7
        elif bb == 7:
            # 1010:54FA  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:54FD  56           push r16
            cpu.push(s.si)
            # 1010:54FE  e85807       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x5C59, 0x5501)
            bb = 8
        elif bb == 8:
            # 1010:5501  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:5504  5f           pop r16
            s.di = cpu.pop()
            # 1010:5505  5e           pop r16
            s.si = cpu.pop()
            # 1010:5506  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:5507  c3           ret
            cpu.instruction_count += 5
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5481.owns_time = True

