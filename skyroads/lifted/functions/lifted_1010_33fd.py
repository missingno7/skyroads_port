"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:33FD  (48 instructions, 14 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x33FD)
SIGNATURE = bytes.fromhex('8cd08ed833d2a1340eb90500f7f13d05')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x33FD, 1: 0x3410, 2: 0x343F, 3: 0x3442, 4: 0x3447, 5: 0x344E, 6: 0x345A, 7: 0x345C, 8: 0x3460, 9: 0x3464, 10: 0x3466, 11: 0x3469, 12: 0x3472, 13: 0x347D}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_33fd(cpu, bb=0):
    """Lifted replacement for 1010:33FD."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x33FD, SIGNATURE, 'lifted_1010_33fd')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:33FD  8cd0         mov rm16,sreg
            s.ax = (s.ss) & 0xFFFF
            # 1010:33FF  8ed8         mov sreg,rm16
            cpu.set_sreg(3, s.ax)
            # 1010:3401  33d2         xor
            _a = s.dx
            _b = s.dx
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:3403  a1340e       mov moffs
            s.ax = mem.rw(s.ds, 0x0E34)
            # 1010:3406  b90500       mov r16,imm16
            s.cx = 0x5
            # 1010:3409  f7f1         grp3 rm16
            _v = s.cx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:340B  3d0500       cmp
            _a = s.ax
            _b = 0x5
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:340E  736d         jnb
            cpu.instruction_count += 8
            bb = 13 if cpu.condition(0x3) else 1
        elif bb == 1:
            # 1010:3410  b90501       mov r16,imm16
            s.cx = 0x105
            # 1010:3413  f7e1         grp3 rm16
            _v = s.cx
            _result = (s.ax & 0xFFFF) * (_v & 0xFFFF)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = s.dx != 0
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:3415  be8e06       mov r16,imm16
            s.si = 0x68E
            # 1010:3418  03f0         add
            _a = s.si
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:341A  b89d00       mov r16,imm16
            s.ax = 0x9D
            # 1010:341D  2b062c0e     sub
            _o = 0x0E2C
            _a = s.ax
            _b = mem.rw(s.ds, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:3421  051000       add
            _a = s.ax
            _b = 0x10
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:3424  0306340e     add
            _o = 0x0E34
            _a = s.ax
            _b = mem.rw(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:3428  b94001       mov r16,imm16
            s.cx = 0x140
            # 1010:342B  f7e9         grp3 rm16
            _v = s.cx
            _result = cpu.sign16(s.ax) * cpu.sign16(_v)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = not (-32768 <= _result <= 32767)
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:342D  8bf8         mov
            s.di = (s.ax) & 0xFFFF
            # 1010:342F  033e280e     add
            _o = 0x0E28
            _a = s.di
            _b = mem.rw(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:3433  83ef6e       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x6E
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:3436  893e700e     mov
            _o = 0x0E70
            mem.ww(s.ds, _o, s.di)
            # 1010:343A  33db         xor
            _a = s.bx
            _b = s.bx
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 16)
            s.bx = (_r & 0xFFFF) & 0xFFFF
            # 1010:343C  ba1d00       mov r16,imm16
            s.dx = 0x1D
            cpu.instruction_count += 16
            bb = 2
        elif bb == 2:
            # 1010:343F  b90900       mov r16,imm16
            s.cx = 0x9
            cpu.instruction_count += 1
            bb = 3
        elif bb == 3:
            # 1010:3442  803800       grp1 rm8,imm8
            _o = (s.bx + s.si) & 0xFFFF
            _a = mem.rb(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3445  7422         jz
            cpu.instruction_count += 2
            bb = 11 if cpu.condition(0x4) else 4
        elif bb == 4:
            # 1010:3447  80bf3e1100   grp1 rm8,imm8
            _o = (s.bx + 0x113E) & 0xFFFF
            _a = mem.rb(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:344C  741b         jz
            cpu.instruction_count += 2
            bb = 11 if cpu.condition(0x4) else 5
        elif bb == 5:
            # 1010:344E  c6873e1102   mov rm8,imm8
            _o = (s.bx + 0x113E) & 0xFFFF
            mem.wb(s.ds, _o, 0x2)
            # 1010:3453  268a05       mov
            _o = (s.di) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.es, _o))
            # 1010:3456  3c3d         cmp
            _a = cpu.get_reg8(0)
            _b = 0x3D
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3458  7502         jnz
            cpu.instruction_count += 4
            bb = 7 if cpu.condition(0x5) else 6
        elif bb == 6:
            # 1010:345A  b040         mov r8,imm8
            cpu.set_reg8(0, 0x40)
            cpu.instruction_count += 1
            bb = 7
        elif bb == 7:
            # 1010:345C  3c00         cmp
            _a = cpu.get_reg8(0)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:345E  7406         jz
            cpu.instruction_count += 2
            bb = 10 if cpu.condition(0x4) else 8
        elif bb == 8:
            # 1010:3460  3c10         cmp
            _a = cpu.get_reg8(0)
            _b = 0x10
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3462  7302         jnb
            cpu.instruction_count += 2
            bb = 10 if cpu.condition(0x3) else 9
        elif bb == 9:
            # 1010:3464  042d         add
            _a = cpu.get_reg8(0)
            _b = 0x2D
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            cpu.instruction_count += 1
            bb = 10
        elif bb == 10:
            # 1010:3466  268805       mov
            _o = (s.di) & 0xFFFF
            mem.wb(s.es, _o, cpu.get_reg8(0))
            cpu.instruction_count += 1
            bb = 11
        elif bb == 11:
            # 1010:3469  81c74001     grp1 rm16,imm16
            _a = s.di
            _b = 0x140
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:346D  83c31d       grp1 rm16,imm8sx
            _a = s.bx
            _b = 0x1D
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.bx = (_r & 0xFFFF) & 0xFFFF
            # 1010:3470  e2d0         loop
            cpu.instruction_count += 3
            s.cx = (s.cx - 1) & 0xFFFF
            bb = 3 if (s.cx != 0) else 12
        elif bb == 12:
            # 1010:3472  81ef3f0b     grp1 rm16,imm16
            _a = s.di
            _b = 0xB3F
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:3476  81eb0401     grp1 rm16,imm16
            _a = s.bx
            _b = 0x104
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.bx = (_r & 0xFFFF) & 0xFFFF
            # 1010:347A  4a           dec r16
            _old = s.dx
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.dx = _r
            # 1010:347B  75c2         jnz
            cpu.instruction_count += 4
            bb = 2 if cpu.condition(0x5) else 13
        elif bb == 13:
            # 1010:347D  c3           ret
            cpu.instruction_count += 1
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_33fd.owns_time = True

