"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5B39  (29 instructions, 1 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5B39)
SIGNATURE = bytes.fromhex('a1b80c0c04e60a345ce60bb000e60c8b')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5B39}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_5b39(cpu, bb=0):
    """Lifted replacement for 1010:5B39."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5B39, SIGNATURE, 'lifted_1010_5b39')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5B39  a1b80c       mov moffs
            s.ax = mem.rw(s.ds, 0x0CB8)
            # 1010:5B3C  0c04         or
            _a = cpu.get_reg8(0)
            _b = 0x4
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5B3E  e60a         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x0A) & 0xFFFF, _val, 8)
            # 1010:5B40  345c         xor
            _a = cpu.get_reg8(0)
            _b = 0x5C
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5B42  e60b         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x0B) & 0xFFFF, _val, 8)
            # 1010:5B44  b000         mov r8,imm8
            cpu.set_reg8(0, 0x0)
            # 1010:5B46  e60c         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x0C) & 0xFFFF, _val, 8)
            # 1010:5B48  8bc3         mov
            s.ax = (s.bx) & 0xFFFF
            # 1010:5B4A  c1eb0c       shift rm16,imm8
            _r = cpu.shift(5, s.bx, 0x0C, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:5B4D  c1e004       shift rm16,imm8
            _r = cpu.shift(4, s.ax, 0x04, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:5B50  03c2         add
            _a = s.ax
            _b = s.dx
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:5B52  80d300       grp1 rm8,imm8
            _a = cpu.get_reg8(3)
            _b = 0x0
            _c = 1 if s.flags & CF else 0
            _r = _a + _b + _c
            cpu.set_add_flags(_a, _b, _r, 8, _c)
            cpu.set_reg8(3, _r & 0xFF)
            # 1010:5B55  8b16b80c     mov
            _o = 0x0CB8
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5B59  d1e2         shift
            _r = cpu.shift(4, s.dx, 1, 16)
            s.dx = (_r) & 0xFFFF
            # 1010:5B5B  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5B5C  8ac4         mov
            cpu.set_reg8(0, cpu.get_reg8(4))
            # 1010:5B5E  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5B5F  42           inc r16
            _old = s.dx
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.dx = _r
            # 1010:5B60  8ac1         mov
            cpu.set_reg8(0, cpu.get_reg8(1))
            # 1010:5B62  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5B63  8ac5         mov
            cpu.set_reg8(0, cpu.get_reg8(5))
            # 1010:5B65  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5B66  8ac3         mov
            cpu.set_reg8(0, cpu.get_reg8(3))
            # 1010:5B68  8b1eb80c     mov
            _o = 0x0CB8
            s.bx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:5B6C  8a97ba0c     mov
            _o = (s.bx + 0xCBA) & 0xFFFF
            cpu.set_reg8(2, mem.rb(s.ds, _o))
            # 1010:5B70  ee           out dx,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 8)
            # 1010:5B71  b001         mov r8,imm8
            cpu.set_reg8(0, 0x1)
            # 1010:5B73  e60a         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x0A) & 0xFFFF, _val, 8)
            # 1010:5B75  c3           ret
            cpu.instruction_count += 29
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5b39.owns_time = True

