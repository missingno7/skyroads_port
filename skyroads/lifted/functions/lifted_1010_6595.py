"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:6595  (36 instructions, 9 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x6595)
SIGNATURE = bytes.fromhex('c8000000065657833eaa41007538')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x6595, 1: 0x65A3, 2: 0x65A9, 3: 0x65AD, 4: 0x65BD, 5: 0x65BF, 6: 0x65CA, 7: 0x65D9, 8: 0x65DB}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:65CA": 6, "1010:65D9": 7}


def lifted_1010_6595(cpu, bb=0):
    """Lifted replacement for 1010:6595."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x6595, SIGNATURE, 'lifted_1010_6595')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:6595  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:6599  06           push es
            cpu.push(s.es)
            # 1010:659A  56           push r16
            cpu.push(s.si)
            # 1010:659B  57           push r16
            cpu.push(s.di)
            # 1010:659C  833eaa4100   grp1 rm16,imm8sx
            _o = 0x41AA
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:65A1  7538         jnz
            cpu.instruction_count += 6
            bb = 8 if cpu.condition(0x5) else 1
        elif bb == 1:
            # 1010:65A3  c47e04       les
            _o = (s.bp + 0x4) & 0xFFFF
            _off = mem.rw(s.ss, _o)
            _seg = mem.rw(s.ss, (_o + 2) & 0xFFFF)
            s.di = _off
            s.es = _seg
            # 1010:65A6  8b7608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            cpu.instruction_count += 2
            bb = 2
        elif bb == 2:
            # 1010:65A9  0bf6         or
            _a = s.si
            _b = s.si
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:65AB  742e         jz
            cpu.instruction_count += 2
            bb = 8 if cpu.condition(0x4) else 3
        elif bb == 3:
            # 1010:65AD  4e           dec r16
            _old = s.si
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.si = _r
            # 1010:65AE  a1b041       mov moffs
            s.ax = mem.rw(s.ds, 0x41B0)
            # 1010:65B1  aa           stos/lods/scas
            cpu.string_op(0xAA, None, None)
            # 1010:65B2  a1b441       mov moffs
            s.ax = mem.rw(s.ds, 0x41B4)
            # 1010:65B5  2b06b641     sub
            _o = 0x41B6
            _a = s.ax
            _b = mem.rw(s.ds, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:65B9  3bc6         cmp
            _a = s.ax
            _b = s.si
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:65BB  7202         jb
            cpu.instruction_count += 7
            bb = 5 if cpu.condition(0x2) else 4
        elif bb == 4:
            # 1010:65BD  8bc6         mov
            s.ax = (s.si) & 0xFFFF
            cpu.instruction_count += 1
            bb = 5
        elif bb == 5:
            # 1010:65BF  50           push r16
            cpu.push(s.ax)
            # 1010:65C0  1e           push ds
            cpu.push(s.ds)
            # 1010:65C1  ff36b641     push rm16
            _o = 0x41B6
            cpu.push(mem.rw(s.ds, _o))
            # 1010:65C5  06           push es
            cpu.push(s.es)
            # 1010:65C6  57           push r16
            cpu.push(s.di)
            # 1010:65C7  e889fa       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x6053, 0x65CA)
            bb = 6
        elif bb == 6:
            # 1010:65CA  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:65CD  58           pop r16
            s.ax = cpu.pop()
            # 1010:65CE  03f8         add
            _a = s.di
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:65D0  0106b641     add
            _o = 0x41B6
            _a = mem.rw(s.ds, _o)
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ds, _o, _r & 0xFFFF)
            # 1010:65D4  2bf0         sub
            _a = s.si
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:65D6  e84dfd       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x6326, 0x65D9)
            bb = 7
        elif bb == 7:
            # 1010:65D9  ebce         jmp short
            cpu.instruction_count += 1
            bb = 2
        elif bb == 8:
            # 1010:65DB  5f           pop r16
            s.di = cpu.pop()
            # 1010:65DC  5e           pop r16
            s.si = cpu.pop()
            # 1010:65DD  07           pop es
            cpu.set_sreg(0, cpu.pop())
            # 1010:65DE  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:65DF  c3           ret
            cpu.instruction_count += 5
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_6595.owns_time = True

