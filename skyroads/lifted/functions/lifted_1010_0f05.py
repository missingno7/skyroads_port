"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:0F05  (32 instructions, 13 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x0F05)
SIGNATURE = bytes.fromhex('c80000005657c7062caf0000c7060016')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x0F05, 1: 0x0F1A, 2: 0x0F1D, 3: 0x0F28, 4: 0x0F2B, 5: 0x0F31, 6: 0x0F36, 7: 0x0F3C, 8: 0x0F3F, 9: 0x0F4E, 10: 0x0F55, 11: 0x0F58, 12: 0x0F5E}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:0F1A": 1, "1010:0F1D": 2, "1010:0F36": 6, "1010:0F3C": 7, "1010:0F3F": 8}


def lifted_1010_0f05(cpu, bb=0):
    """Lifted replacement for 1010:0F05."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x0F05, SIGNATURE, 'lifted_1010_0f05')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:0F05  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:0F09  56           push r16
            cpu.push(s.si)
            # 1010:0F0A  57           push r16
            cpu.push(s.di)
            # 1010:0F0B  c7062caf0000 mov rm16,imm16
            _o = 0xAF2C
            mem.ww(s.ds, _o, 0x0)
            # 1010:0F11  c70600160000 mov rm16,imm16
            _o = 0x1600
            mem.ww(s.ds, _o, 0x0)
            # 1010:0F17  e93400       jmp
            cpu.instruction_count += 6
            bb = 9
        elif bb == 1:
            # 1010:0F1A  a0da0b       mov moffs
            s.ax = (s.ax & 0xFF00) | mem.rb(s.ds, 0x0BDA)
            cpu.instruction_count += 1
            if cpu.boundary_hook is not None:
                cpu.boundary_hook(cpu, 0x1010, 0x0F1A, 0x0F1D)
            bb = 2
        elif bb == 2:
            # 1010:0F1D  25ff00       and
            _a = s.ax
            _b = 0xFF
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0F20  258000       and
            _a = s.ax
            _b = 0x80
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0F23  3d0000       cmp
            _a = s.ax
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0F26  7503         jnz
            cpu.instruction_count += 4
            bb = 4 if cpu.condition(0x5) else 3
        elif bb == 3:
            # 1010:0F28  e90600       jmp
            cpu.instruction_count += 1
            bb = 5
        elif bb == 4:
            # 1010:0F2B  b80700       mov r16,imm16
            s.ax = 0x7
            # 1010:0F2E  e92d00       jmp
            cpu.instruction_count += 2
            bb = 12
        elif bb == 5:
            # 1010:0F31  6a00         push imm8
            cpu.push(0x0000)
            # 1010:0F33  e862fd       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x0C98, 0x0F36)
            bb = 6
        elif bb == 6:
            # 1010:0F36  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:0F39  e8afff       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x0EEB, 0x0F3C)
            bb = 7
        elif bb == 7:
            # 1010:0F3C  e891ff       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x0ED0, 0x0F3F)
            bb = 8
        elif bb == 8:
            # 1010:0F3F  a1ac54       mov moffs
            s.ax = mem.rw(s.ds, 0x54AC)
            # 1010:0F42  8b16ae54     mov
            _o = 0x54AE
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:0F46  01061896     add
            _o = 0x9618
            _a = mem.rw(s.ds, _o)
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ds, _o, _r & 0xFFFF)
            # 1010:0F4A  11161a96     adc
            _o = 0x961A
            _a = mem.rw(s.ds, _o)
            _b = s.dx
            _c = 1 if s.flags & CF else 0
            _r = _a + _b + _c
            cpu.set_add_flags(_a, _b, _r, 16, _c)
            mem.ww(s.ds, _o, _r & 0xFFFF)
            cpu.instruction_count += 4
            bb = 9
        elif bb == 9:
            # 1010:0F4E  833e001648   grp1 rm16,imm8sx
            _o = 0x1600
            _a = mem.rw(s.ds, _o)
            _b = 0x48
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0F53  7303         jnb
            cpu.instruction_count += 2
            bb = 11 if cpu.condition(0x3) else 10
        elif bb == 10:
            # 1010:0F55  e9c2ff       jmp
            cpu.instruction_count += 1
            bb = 1
        elif bb == 11:
            # 1010:0F58  b80000       mov r16,imm16
            s.ax = 0x0
            # 1010:0F5B  e90000       jmp
            cpu.instruction_count += 2
            bb = 12
        elif bb == 12:
            # 1010:0F5E  5f           pop r16
            s.di = cpu.pop()
            # 1010:0F5F  5e           pop r16
            s.si = cpu.pop()
            # 1010:0F60  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:0F61  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_0f05.owns_time = True

