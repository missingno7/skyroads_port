"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5508  (38 instructions, 8 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5508)
SIGNATURE = bytes.fromhex('c802000056576874546a0a68f89568ba')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5508, 1: 0x551C, 2: 0x552D, 3: 0x553E, 4: 0x5547, 5: 0x5556, 6: 0x555C, 7: 0x5560}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:551C": 1, "1010:552D": 2, "1010:553E": 3, "1010:5547": 4, "1010:5556": 5, "1010:555C": 6, "1010:5560": 7}


def lifted_1010_5508(cpu, bb=0):
    """Lifted replacement for 1010:5508."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5508, SIGNATURE, 'lifted_1010_5508')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5508  c8020000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x2) & 0xFFFF
            # 1010:550C  56           push r16
            cpu.push(s.si)
            # 1010:550D  57           push r16
            cpu.push(s.di)
            # 1010:550E  687454       push imm16
            cpu.push(0x5474)
            # 1010:5511  6a0a         push imm8
            cpu.push(0x000A)
            # 1010:5513  68f895       push imm16
            cpu.push(0x95F8)
            # 1010:5516  68ba0d       push imm16
            cpu.push(0xDBA)
            # 1010:5519  e865ff       call
            cpu.instruction_count += 8
            emulate_call(cpu, 0x1010, 0x5481, 0x551C)
            bb = 1
        elif bb == 1:
            # 1010:551C  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:551F  680e96       push imm16
            cpu.push(0x960E)
            # 1010:5522  6a0a         push imm8
            cpu.push(0x000A)
            # 1010:5524  688054       push imm16
            cpu.push(0x5480)
            # 1010:5527  68c80d       push imm16
            cpu.push(0xDC8)
            # 1010:552A  e854ff       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x5481, 0x552D)
            bb = 2
        elif bb == 2:
            # 1010:552D  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:5530  68a454       push imm16
            cpu.push(0x54A4)
            # 1010:5533  6a22         push imm8
            cpu.push(0x0022)
            # 1010:5535  687245       push imm16
            cpu.push(0x4572)
            # 1010:5538  68d60d       push imm16
            cpu.push(0xDD6)
            # 1010:553B  e843ff       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x5481, 0x553E)
            bb = 3
        elif bb == 3:
            # 1010:553E  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:5541  68e00d       push imm16
            cpu.push(0xDE0)
            # 1010:5544  e83007       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x5C77, 0x5547)
            bb = 4
        elif bb == 4:
            # 1010:5547  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:554A  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:554C  68fe18       push imm16
            cpu.push(0x18FE)
            # 1010:554F  681e96       push imm16
            cpu.push(0x961E)
            # 1010:5552  56           push r16
            cpu.push(s.si)
            # 1010:5553  e8270a       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x5F7D, 0x5556)
            bb = 5
        elif bb == 5:
            # 1010:5556  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:5559  e8b506       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x5C11, 0x555C)
            bb = 6
        elif bb == 6:
            # 1010:555C  56           push r16
            cpu.push(s.si)
            # 1010:555D  e8f906       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x5C59, 0x5560)
            bb = 7
        elif bb == 7:
            # 1010:5560  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:5563  5f           pop r16
            s.di = cpu.pop()
            # 1010:5564  5e           pop r16
            s.si = cpu.pop()
            # 1010:5565  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:5566  c3           ret
            cpu.instruction_count += 5
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5508.owns_time = True

