"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5DEE  (48 instructions, 11 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5DEE)
SIGNATURE = bytes.fromhex('558bec538b460a0bc07515')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5DEE, 1: 0x5DF9, 2: 0x5E0E, 3: 0x5E19, 4: 0x5E25, 5: 0x5E34, 6: 0x5E39, 7: 0x5E3B, 8: 0x5E40, 9: 0x5E46, 10: 0x5E53}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_5dee(cpu, bb=0):
    """Lifted replacement for 1010:5DEE."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5DEE, SIGNATURE, 'lifted_1010_5dee')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5DEE  55           push r16
            cpu.push(s.bp)
            # 1010:5DEF  8bec         mov
            s.bp = (s.sp) & 0xFFFF
            # 1010:5DF1  53           push r16
            cpu.push(s.bx)
            # 1010:5DF2  8b460a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5DF5  0bc0         or
            _a = s.ax
            _b = s.ax
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:5DF7  7515         jnz
            cpu.instruction_count += 6
            bb = 2 if cpu.condition(0x5) else 1
        elif bb == 1:
            # 1010:5DF9  8b4e08       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5DFC  8b4606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5DFF  33d2         xor
            _a = s.dx
            _b = s.dx
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E01  f7f1         grp3 rm16
            _v = s.cx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:5E03  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5E06  f7f1         grp3 rm16
            _v = s.cx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:5E08  8bc2         mov
            s.ax = (s.dx) & 0xFFFF
            # 1010:5E0A  33d2         xor
            _a = s.dx
            _b = s.dx
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E0C  eb45         jmp short
            cpu.instruction_count += 9
            bb = 10
        elif bb == 2:
            # 1010:5E0E  8bc8         mov
            s.cx = (s.ax) & 0xFFFF
            # 1010:5E10  8b5e08       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5E13  8b5606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5E16  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            cpu.instruction_count += 4
            bb = 3
        elif bb == 3:
            # 1010:5E19  d1e9         shift
            _r = cpu.shift(5, s.cx, 1, 16)
            s.cx = (_r) & 0xFFFF
            # 1010:5E1B  d1db         shift
            _r = cpu.shift(3, s.bx, 1, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:5E1D  d1ea         shift
            _r = cpu.shift(5, s.dx, 1, 16)
            s.dx = (_r) & 0xFFFF
            # 1010:5E1F  d1d8         shift
            _r = cpu.shift(3, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:5E21  0bc9         or
            _a = s.cx
            _b = s.cx
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E23  75f4         jnz
            cpu.instruction_count += 6
            bb = 3 if cpu.condition(0x5) else 4
        elif bb == 4:
            # 1010:5E25  f7f3         grp3 rm16
            _v = s.bx
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(((s.dx & 0xFFFF) << 16) | (s.ax & 0xFFFF), _v & 0xFFFF)
            if _q > 0xFFFF:
                raise OverflowError('16-bit div quotient overflow')
            s.ax = _q & 0xFFFF
            s.dx = _r & 0xFFFF
            # 1010:5E27  8bc8         mov
            s.cx = (s.ax) & 0xFFFF
            # 1010:5E29  f7660a       grp3 rm16
            _o = (s.bp + 0xA) & 0xFFFF
            _v = mem.rw(s.ss, _o)
            _result = (s.ax & 0xFFFF) * (_v & 0xFFFF)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = s.dx != 0
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:5E2C  91           xchg ax,r16
            s.ax, s.cx = s.cx, s.ax
            # 1010:5E2D  f76608       grp3 rm16
            _o = (s.bp + 0x8) & 0xFFFF
            _v = mem.rw(s.ss, _o)
            _result = (s.ax & 0xFFFF) * (_v & 0xFFFF)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = s.dx != 0
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:5E30  03d1         add
            _a = s.dx
            _b = s.cx
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E32  720c         jb
            cpu.instruction_count += 7
            bb = 8 if cpu.condition(0x2) else 5
        elif bb == 5:
            # 1010:5E34  3b5606       cmp
            _o = (s.bp + 0x6) & 0xFFFF
            _a = s.dx
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5E37  7707         ja
            cpu.instruction_count += 2
            bb = 8 if cpu.condition(0x7) else 6
        elif bb == 6:
            # 1010:5E39  720b         jb
            cpu.instruction_count += 1
            bb = 9 if cpu.condition(0x2) else 7
        elif bb == 7:
            # 1010:5E3B  3b4604       cmp
            _o = (s.bp + 0x4) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5E3E  7606         jbe
            cpu.instruction_count += 2
            bb = 9 if cpu.condition(0x6) else 8
        elif bb == 8:
            # 1010:5E40  2b4608       sub
            _o = (s.bp + 0x8) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E43  1b560a       sbb
            _o = (s.bp + 0xA) & 0xFFFF
            _a = s.dx
            _b = mem.rw(s.ss, _o)
            _c = 1 if s.flags & CF else 0
            _r = _a - _b - _c
            cpu.set_sub_flags(_a, _b, _r, 16, _c)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 2
            bb = 9
        elif bb == 9:
            # 1010:5E46  2b4604       sub
            _o = (s.bp + 0x4) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E49  1b5606       sbb
            _o = (s.bp + 0x6) & 0xFFFF
            _a = s.dx
            _b = mem.rw(s.ss, _o)
            _c = 1 if s.flags & CF else 0
            _r = _a - _b - _c
            cpu.set_sub_flags(_a, _b, _r, 16, _c)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5E4C  f7da         grp3 rm16
            _v = s.dx
            cpu.set_sub_flags(0, _v, -_v, 16)
            s.dx = ((-_v) & 0xFFFF) & 0xFFFF
            # 1010:5E4E  f7d8         grp3 rm16
            _v = s.ax
            cpu.set_sub_flags(0, _v, -_v, 16)
            s.ax = ((-_v) & 0xFFFF) & 0xFFFF
            # 1010:5E50  83da00       grp1 rm16,imm8sx
            _a = s.dx
            _b = 0x0
            _c = 1 if s.flags & CF else 0
            _r = _a - _b - _c
            cpu.set_sub_flags(_a, _b, _r, 16, _c)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 5
            bb = 10
        elif bb == 10:
            # 1010:5E53  5b           pop r16
            s.bx = cpu.pop()
            # 1010:5E54  8be5         mov
            s.sp = (s.bp) & 0xFFFF
            # 1010:5E56  5d           pop r16
            s.bp = cpu.pop()
            # 1010:5E57  c20800       ret imm16
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            s.sp = (s.sp + 0x8) & 0xFFFF
            cpu.call_depth = max(0, cpu.call_depth - 1)
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5dee.owns_time = True
