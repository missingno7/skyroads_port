"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5919  (33 instructions, 10 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5919)
SIGNATURE = bytes.fromhex('50e8b200')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5919, 1: 0x591D, 2: 0x5937, 3: 0x5944, 4: 0x594A, 5: 0x5956, 6: 0x5959, 7: 0x595F, 8: 0x5967, 9: 0x5970}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:591D": 1, "1010:5944": 3, "1010:5959": 6, "1010:5970": 9}


def lifted_1010_5919(cpu, bb=0):
    """Lifted replacement for 1010:5919."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5919, SIGNATURE, 'lifted_1010_5919')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5919  50           push r16
            cpu.push(s.ax)
            # 1010:591A  e8b200       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x59CF, 0x591D)
            bb = 1
        elif bb == 1:
            # 1010:591D  58           pop r16
            s.ax = cpu.pop()
            # 1010:591E  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:5920  81e600ff     grp1 rm16,imm16
            _a = s.si
            _b = 0xFF00
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:5924  c1ee04       shift rm16,imm8
            _r = cpu.shift(5, s.si, 0x04, 16)
            s.si = (_r) & 0xFFFF
            # 1010:5927  03369431     add
            _o = 0x3194
            _a = s.si
            _b = mem.rw(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:592B  8bf8         mov
            s.di = (s.ax) & 0xFFFF
            # 1010:592D  81e7ff00     grp1 rm16,imm16
            _a = s.di
            _b = 0xFF
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:5931  88a59b31     mov
            _o = (s.di + 0x319B) & 0xFFFF
            mem.wb(s.ds, _o, cpu.get_reg8(4))
            # 1010:5935  33db         xor
            _a = s.bx
            _b = s.bx
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 16)
            s.bx = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 9
            bb = 2
        elif bb == 2:
            # 1010:5937  8a20         mov
            _o = (s.bx + s.si) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ds, _o))
            # 1010:5939  8a851b0c     mov
            _o = (s.di + 0xC1B) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ds, _o))
            # 1010:593D  0287100c     add
            _o = (s.bx + 0xC10) & 0xFFFF
            _a = cpu.get_reg8(0)
            _b = mem.rb(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5941  e84eff       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x5892, 0x5944)
            bb = 3
        elif bb == 3:
            # 1010:5944  43           inc r16
            _old = s.bx
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.bx = _r
            # 1010:5945  83fb05       grp1 rm16,imm8sx
            _a = s.bx
            _b = 0x5
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:5948  72ed         jb
            cpu.instruction_count += 3
            bb = 2 if cpu.condition(0x2) else 4
        elif bb == 4:
            # 1010:594A  8a20         mov
            _o = (s.bx + s.si) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ds, _o))
            # 1010:594C  8a85260c     mov
            _o = (s.di + 0xC26) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ds, _o))
            # 1010:5950  0287100c     add
            _o = (s.bx + 0xC10) & 0xFFFF
            _a = cpu.get_reg8(0)
            _b = mem.rb(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5954  7203         jb
            cpu.instruction_count += 4
            bb = 6 if cpu.condition(0x2) else 5
        elif bb == 5:
            # 1010:5956  e839ff       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x5892, 0x5959)
            bb = 6
        elif bb == 6:
            # 1010:5959  43           inc r16
            _old = s.bx
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.bx = _r
            # 1010:595A  83fb0a       grp1 rm16,imm8sx
            _a = s.bx
            _b = 0xA
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:595D  72eb         jb
            cpu.instruction_count += 3
            bb = 4 if cpu.condition(0x2) else 7
        elif bb == 7:
            # 1010:595F  8a85310c     mov
            _o = (s.di + 0xC31) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ds, _o))
            # 1010:5963  3cff         cmp
            _a = cpu.get_reg8(0)
            _b = 0xFF
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:5965  7409         jz
            cpu.instruction_count += 3
            bb = 9 if cpu.condition(0x4) else 8
        elif bb == 8:
            # 1010:5967  0287100c     add
            _o = (s.bx + 0xC10) & 0xFFFF
            _a = cpu.get_reg8(0)
            _b = mem.rb(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:596B  8a20         mov
            _o = (s.bx + s.si) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ds, _o))
            # 1010:596D  e822ff       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x5892, 0x5970)
            bb = 9
        elif bb == 9:
            # 1010:5970  c3           ret
            cpu.instruction_count += 1
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5919.owns_time = True

