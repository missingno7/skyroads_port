"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:03C2  (71 instructions, 9 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x03C2)
SIGNATURE = bytes.fromhex('c80a00005657a10016a338af833e1a45')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x03C2, 1: 0x03D5, 2: 0x03D8, 3: 0x03DF, 4: 0x03E2, 5: 0x0439, 6: 0x0458, 7: 0x045E, 8: 0x0472}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:0439": 5, "1010:0458": 6}


def lifted_1010_03c2(cpu, bb=0):
    """Lifted replacement for 1010:03C2."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x03C2, SIGNATURE, 'lifted_1010_03c2')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:03C2  c80a0000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0xA) & 0xFFFF
            # 1010:03C6  56           push r16
            cpu.push(s.si)
            # 1010:03C7  57           push r16
            cpu.push(s.di)
            # 1010:03C8  a10016       mov moffs
            s.ax = mem.rw(s.ds, 0x1600)
            # 1010:03CB  a338af       mov moffs
            mem.ww(s.ds, 0xAF38, s.ax)
            # 1010:03CE  833e1a4500   grp1 rm16,imm8sx
            _o = 0x451A
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:03D3  7403         jz
            cpu.instruction_count += 7
            bb = 2 if cpu.condition(0x4) else 1
        elif bb == 1:
            # 1010:03D5  e99a00       jmp
            cpu.instruction_count += 1
            bb = 8
        elif bb == 2:
            # 1010:03D8  833eb60c00   grp1 rm16,imm8sx
            _o = 0x0CB6
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:03DD  7503         jnz
            cpu.instruction_count += 2
            bb = 4 if cpu.condition(0x5) else 3
        elif bb == 3:
            # 1010:03DF  e97c00       jmp
            cpu.instruction_count += 1
            bb = 7
        elif bb == 4:
            # 1010:03E2  a16045       mov moffs
            s.ax = mem.rw(s.ds, 0x4560)
            # 1010:03E5  8946fc       mov
            _o = (s.bp - 0x4) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:03E8  c746fa0000   mov rm16,imm16
            _o = (s.bp - 0x6) & 0xFFFF
            mem.ww(s.ss, _o, 0x0)
            # 1010:03ED  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:03F0  050100       add
            _a = s.ax
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:03F3  d1e0         shift
            _r = cpu.shift(4, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:03F5  8b4efa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:03F8  8b56fc       mov
            _o = (s.bp - 0x4) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:03FB  03c8         add
            _a = s.cx
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:03FD  8bd9         mov
            s.bx = (s.cx) & 0xFFFF
            # 1010:03FF  8ec2         mov sreg,rm16
            cpu.set_sreg(0, s.dx)
            # 1010:0401  268b07       mov
            _o = (s.bx) & 0xFFFF
            s.ax = (mem.rw(s.es, _o)) & 0xFFFF
            # 1010:0404  8b5604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:0407  d1e2         shift
            _r = cpu.shift(4, s.dx, 1, 16)
            s.dx = (_r) & 0xFFFF
            # 1010:0409  8b4efa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:040C  8b5efc       mov
            _o = (s.bp - 0x4) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:040F  03ca         add
            _a = s.cx
            _b = s.dx
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:0411  895ef6       mov
            _o = (s.bp - 0xA) & 0xFFFF
            mem.ww(s.ss, _o, s.bx)
            # 1010:0414  8bd9         mov
            s.bx = (s.cx) & 0xFFFF
            # 1010:0416  8e46f6       mov sreg,rm16
            _o = (s.bp - 0xA) & 0xFFFF
            cpu.set_sreg(0, mem.rw(s.ss, _o))
            # 1010:0419  262b07       sub
            _o = (s.bx) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.es, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:041C  8946fe       mov
            _o = (s.bp - 0x2) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:041F  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:0422  d1e0         shift
            _r = cpu.shift(4, s.ax, 1, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:0424  8b4efa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            s.cx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:0427  8b56fc       mov
            _o = (s.bp - 0x4) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:042A  03c8         add
            _a = s.cx
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:042C  8bd9         mov
            s.bx = (s.cx) & 0xFFFF
            # 1010:042E  8ec2         mov sreg,rm16
            cpu.set_sreg(0, s.dx)
            # 1010:0430  268b07       mov
            _o = (s.bx) & 0xFFFF
            s.ax = (mem.rw(s.es, _o)) & 0xFFFF
            # 1010:0433  8946fa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:0436  e88757       call
            cpu.instruction_count += 32
            emulate_call(cpu, 0x1010, 0x5BC0, 0x0439)
            bb = 5
        elif bb == 5:
            # 1010:0439  8b46fe       mov
            _o = (s.bp - 0x2) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:043C  05ffff       add
            _a = s.ax
            _b = 0xFFFF
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:043F  50           push r16
            cpu.push(s.ax)
            # 1010:0440  c45efa       les
            _o = (s.bp - 0x6) & 0xFFFF
            _off = mem.rw(s.ss, _o)
            _seg = mem.rw(s.ss, (_o + 2) & 0xFFFF)
            s.bx = _off
            s.es = _seg
            # 1010:0443  268a07       mov
            _o = (s.bx) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.es, _o))
            # 1010:0446  25ff00       and
            _a = s.ax
            _b = 0xFF
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0449  50           push r16
            cpu.push(s.ax)
            # 1010:044A  8b46fa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:044D  8b56fc       mov
            _o = (s.bp - 0x4) & 0xFFFF
            s.dx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:0450  050100       add
            _a = s.ax
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0453  52           push r16
            cpu.push(s.dx)
            # 1010:0454  50           push r16
            cpu.push(s.ax)
            # 1010:0455  e81e57       call
            cpu.instruction_count += 13
            emulate_call(cpu, 0x1010, 0x5B76, 0x0458)
            bb = 6
        elif bb == 6:
            # 1010:0458  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:045B  e91400       jmp
            cpu.instruction_count += 2
            bb = 8
        elif bb == 7:
            # 1010:045E  fa           cli
            cpu.set_flag(IF, False)
            # 1010:045F  e461         in al,imm8
            _val = cpu.port_reader(cpu, (0x61) & 0xFFFF, 8) if cpu.port_reader else 0
            cpu.set_reg8(0, _val)
            # 1010:0461  0c03         or
            _a = cpu.get_reg8(0)
            _b = 0x3
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:0463  e661         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x61) & 0xFFFF, _val, 8)
            # 1010:0465  fb           sti
            cpu.set_flag(IF, True)
            # 1010:0466  8b5e04       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.bx = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:0469  d1e3         shift
            _r = cpu.shift(4, s.bx, 1, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:046B  8b876201     mov
            _o = (s.bx + 0x162) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:046F  a3d00b       mov moffs
            mem.ww(s.ds, 0x0BD0, s.ax)
            cpu.instruction_count += 9
            bb = 8
        elif bb == 8:
            # 1010:0472  5f           pop r16
            s.di = cpu.pop()
            # 1010:0473  5e           pop r16
            s.si = cpu.pop()
            # 1010:0474  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:0475  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_03c2.owns_time = True

