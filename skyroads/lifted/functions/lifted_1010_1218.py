"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:1218  (46 instructions, 16 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x1218)
SIGNATURE = bytes.fromhex('c806000056578b4604052a008bf0bf8f')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x1218, 1: 0x122E, 2: 0x1234, 3: 0x1239, 4: 0x1241, 5: 0x1244, 6: 0x124A, 7: 0x1254, 8: 0x125C, 9: 0x125F, 10: 0x1266, 11: 0x1269, 12: 0x126F, 13: 0x1272, 14: 0x1278, 15: 0x127E}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:122E": 1, "1010:1239": 3, "1010:1254": 7, "1010:1278": 14}


def lifted_1010_1218(cpu, bb=0):
    """Lifted replacement for 1010:1218."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x1218, SIGNATURE, 'lifted_1010_1218')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:1218  c8060000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x6) & 0xFFFF
            # 1010:121C  56           push r16
            cpu.push(s.si)
            # 1010:121D  57           push r16
            cpu.push(s.di)
            # 1010:121E  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:1221  052a00       add
            _a = s.ax
            _b = 0x2A
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:1224  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:1226  bf8f00       mov r16,imm16
            s.di = 0x8F
            # 1010:1229  57           push r16
            cpu.push(s.di)
            # 1010:122A  56           push r16
            cpu.push(s.si)
            # 1010:122B  e863ff       call
            cpu.instruction_count += 10
            emulate_call(cpu, 0x1010, 0x1191, 0x122E)
            bb = 1
        elif bb == 1:
            # 1010:122E  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:1231  8946fa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            cpu.instruction_count += 2
            bb = 2
        elif bb == 2:
            # 1010:1234  57           push r16
            cpu.push(s.di)
            # 1010:1235  56           push r16
            cpu.push(s.si)
            # 1010:1236  e858ff       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x1191, 0x1239)
            bb = 3
        elif bb == 3:
            # 1010:1239  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:123C  3b46fa       cmp
            _o = (s.bp - 0x6) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:123F  7403         jz
            cpu.instruction_count += 3
            bb = 5 if cpu.condition(0x4) else 4
        elif bb == 4:
            # 1010:1241  e90600       jmp
            cpu.instruction_count += 1
            bb = 6
        elif bb == 5:
            # 1010:1244  83ef01       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x1
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:1247  e9eaff       jmp
            cpu.instruction_count += 2
            bb = 2
        elif bb == 6:
            # 1010:124A  83c701       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:124D  8bc7         mov
            s.ax = (s.di) & 0xFFFF
            # 1010:124F  50           push r16
            cpu.push(s.ax)
            # 1010:1250  56           push r16
            cpu.push(s.si)
            # 1010:1251  e83dff       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x1191, 0x1254)
            bb = 7
        elif bb == 7:
            # 1010:1254  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:1257  3b46fa       cmp
            _o = (s.bp - 0x6) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:125A  7403         jz
            cpu.instruction_count += 3
            bb = 9 if cpu.condition(0x4) else 8
        elif bb == 8:
            # 1010:125C  e91f00       jmp
            cpu.instruction_count += 1
            bb = 15
        elif bb == 9:
            # 1010:125F  833e3c0000   grp1 rm16,imm8sx
            _o = 0x003C
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:1264  7503         jnz
            cpu.instruction_count += 2
            bb = 11 if cpu.condition(0x5) else 10
        elif bb == 10:
            # 1010:1266  e90600       jmp
            cpu.instruction_count += 1
            bb = 12
        elif bb == 11:
            # 1010:1269  b86000       mov r16,imm16
            s.ax = 0x60
            # 1010:126C  e90300       jmp
            cpu.instruction_count += 2
            bb = 13
        elif bb == 12:
            # 1010:126F  b80600       mov r16,imm16
            s.ax = 0x6
            cpu.instruction_count += 1
            bb = 13
        elif bb == 13:
            # 1010:1272  50           push r16
            cpu.push(s.ax)
            # 1010:1273  57           push r16
            cpu.push(s.di)
            # 1010:1274  56           push r16
            cpu.push(s.si)
            # 1010:1275  e85bff       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x11D3, 0x1278)
            bb = 14
        elif bb == 14:
            # 1010:1278  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:127B  e9ccff       jmp
            cpu.instruction_count += 2
            bb = 6
        elif bb == 15:
            # 1010:127E  5f           pop r16
            s.di = cpu.pop()
            # 1010:127F  5e           pop r16
            s.si = cpu.pop()
            # 1010:1280  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:1281  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_1218.owns_time = True

