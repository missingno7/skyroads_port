"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5971  (44 instructions, 9 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5971)
SIGNATURE = bytes.fromhex('50e85a00')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5971, 1: 0x5975, 2: 0x5985, 3: 0x599B, 4: 0x59AC, 5: 0x59B2, 6: 0x59B4, 7: 0x59B7, 8: 0x59CE}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:5975": 1, "1010:599B": 3, "1010:59B2": 5, "1010:59B7": 7, "1010:59CE": 8}


def lifted_1010_5971(cpu, bb=0):
    """Lifted replacement for 1010:5971."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5971, SIGNATURE, 'lifted_1010_5971')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5971  50           push r16
            cpu.push(s.ax)
            # 1010:5972  e85a00       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x59CF, 0x5975)
            bb = 1
        elif bb == 1:
            # 1010:5975  58           pop r16
            s.ax = cpu.pop()
            # 1010:5976  8ad8         mov
            cpu.set_reg8(3, cpu.get_reg8(0))
            # 1010:5978  b700         mov r8,imm8
            cpu.set_reg8(7, 0x0)
            # 1010:597A  50           push r16
            cpu.push(s.ax)
            # 1010:597B  3c07         cmp
            _a = cpu.get_reg8(0)
            _b = 0x7
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:597D  8a87310c     mov
            _o = (s.bx + 0xC31) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ds, _o))
            # 1010:5981  8adc         mov
            cpu.set_reg8(3, cpu.get_reg8(4))
            # 1010:5983  7332         jnb
            cpu.instruction_count += 8
            bb = 7 if cpu.condition(0x3) else 2
        elif bb == 2:
            # 1010:5985  50           push r16
            cpu.push(s.ax)
            # 1010:5986  93           xchg ax,r16
            s.ax, s.bx = s.bx, s.ax
            # 1010:5987  b10c         mov r8,imm8
            cpu.set_reg8(1, 0xC)
            # 1010:5989  f6f1         grp3 rm8
            _v = cpu.get_reg8(1)
            if _v == 0:
                raise ZeroDivisionError('div by zero')
            _q, _r = divmod(s.ax & 0xFFFF, _v & 0xFF)
            if _q > 0xFF:
                raise OverflowError('8-bit div quotient overflow')
            s.ax = ((_r & 0xFF) << 8) | (_q & 0xFF)
            # 1010:598B  0402         add
            _a = cpu.get_reg8(0)
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:598D  50           push r16
            cpu.push(s.ax)
            # 1010:598E  93           xchg ax,r16
            s.ax, s.bx = s.bx, s.ax
            # 1010:598F  04a0         add
            _a = cpu.get_reg8(0)
            _b = 0xA0
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5991  c1eb08       shift rm16,imm8
            _r = cpu.shift(5, s.bx, 0x08, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:5994  8aa76b0c     mov
            _o = (s.bx + 0xC6B) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ds, _o))
            # 1010:5998  e8f7fe       call
            cpu.instruction_count += 11
            emulate_call(cpu, 0x1010, 0x5892, 0x599B)
            bb = 3
        elif bb == 3:
            # 1010:599B  59           pop r16
            s.cx = cpu.pop()
            # 1010:599C  c1e102       shift rm16,imm8
            _r = cpu.shift(4, s.cx, 0x02, 16)
            s.cx = (_r) & 0xFFFF
            # 1010:599F  58           pop r16
            s.ax = cpu.pop()
            # 1010:59A0  8aa7770c     mov
            _o = (s.bx + 0xC77) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ds, _o))
            # 1010:59A4  0ae1         or
            _a = cpu.get_reg8(4)
            _b = cpu.get_reg8(1)
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(4, _r & 0xFF)
            # 1010:59A6  04b0         add
            _a = cpu.get_reg8(0)
            _b = 0xB0
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:59A8  3cb6         cmp
            _a = cpu.get_reg8(0)
            _b = 0xB6
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:59AA  7308         jnb
            cpu.instruction_count += 8
            bb = 6 if cpu.condition(0x3) else 4
        elif bb == 4:
            # 1010:59AC  80cc20       grp1 rm8,imm8
            _a = cpu.get_reg8(4)
            _b = 0x20
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(4, _r & 0xFF)
            # 1010:59AF  e8e0fe       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x5892, 0x59B2)
            bb = 5
        elif bb == 5:
            # 1010:59B2  58           pop r16
            s.ax = cpu.pop()
            # 1010:59B3  c3           ret
            cpu.instruction_count += 2
            s.ip = cpu.pop()
            return
        elif bb == 6:
            # 1010:59B4  e8dbfe       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x5892, 0x59B7)
            bb = 7
        elif bb == 7:
            # 1010:59B7  58           pop r16
            s.ax = cpu.pop()
            # 1010:59B8  8ac8         mov
            cpu.set_reg8(1, cpu.get_reg8(0))
            # 1010:59BA  80e906       grp1 rm8,imm8
            _a = cpu.get_reg8(1)
            _b = 0x6
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            cpu.set_reg8(1, _r & 0xFF)
            # 1010:59BD  b410         mov r8,imm8
            cpu.set_reg8(4, 0x10)
            # 1010:59BF  d2ec         shift
            _r = cpu.shift(5, cpu.get_reg8(4), s.cx & 0xFF, 8)
            cpu.set_reg8(4, _r)
            # 1010:59C1  0a269a31     or
            _o = 0x319A
            _a = cpu.get_reg8(4)
            _b = mem.rb(s.ds, _o)
            _r = _a | _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(4, _r & 0xFF)
            # 1010:59C5  88269a31     mov
            _o = 0x319A
            mem.wb(s.ds, _o, cpu.get_reg8(4))
            # 1010:59C9  b0bd         mov r8,imm8
            cpu.set_reg8(0, 0xBD)
            # 1010:59CB  e8c4fe       call
            cpu.instruction_count += 9
            emulate_call(cpu, 0x1010, 0x5892, 0x59CE)
            bb = 8
        elif bb == 8:
            # 1010:59CE  c3           ret
            cpu.instruction_count += 1
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5971.owns_time = True

