"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:6053  (40 instructions, 5 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x6053)
SIGNATURE = bytes.fromhex('8bdc1e0656578b4f0ac47f02c577068c')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x6053, 1: 0x6086, 2: 0x6088, 3: 0x608C, 4: 0x6093}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_6053(cpu, bb=0):
    """Lifted replacement for 1010:6053."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x6053, SIGNATURE, 'lifted_1010_6053')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:6053  8bdc         mov
            s.bx = (s.sp) & 0xFFFF
            # 1010:6055  1e           push ds
            cpu.push(s.ds)
            # 1010:6056  06           push es
            cpu.push(s.es)
            # 1010:6057  56           push r16
            cpu.push(s.si)
            # 1010:6058  57           push r16
            cpu.push(s.di)
            # 1010:6059  8b4f0a       mov
            _o = (s.bx + 0xA) & 0xFFFF
            s.cx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:605C  c47f02       les
            _o = (s.bx + 0x2) & 0xFFFF
            _off = mem.rw(s.ds, _o)
            _seg = mem.rw(s.ds, (_o + 2) & 0xFFFF)
            s.di = _off
            s.es = _seg
            # 1010:605F  c57706       lds
            _o = (s.bx + 0x6) & 0xFFFF
            _off = mem.rw(s.ds, _o)
            _seg = mem.rw(s.ds, (_o + 2) & 0xFFFF)
            s.si = _off
            s.ds = _seg
            # 1010:6062  8cda         mov rm16,sreg
            s.dx = (s.ds) & 0xFFFF
            # 1010:6064  c1ea0c       shift rm16,imm8
            _r = cpu.shift(5, s.dx, 0x0C, 16)
            s.dx = (_r) & 0xFFFF
            # 1010:6067  8cc0         mov rm16,sreg
            s.ax = (s.es) & 0xFFFF
            # 1010:6069  c1e80c       shift rm16,imm8
            _r = cpu.shift(5, s.ax, 0x0C, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:606C  8af0         mov
            cpu.set_reg8(6, cpu.get_reg8(0))
            # 1010:606E  8cd8         mov rm16,sreg
            s.ax = (s.ds) & 0xFFFF
            # 1010:6070  c1e004       shift rm16,imm8
            _r = cpu.shift(4, s.ax, 0x04, 16)
            s.ax = (_r) & 0xFFFF
            # 1010:6073  03c6         add
            _a = s.ax
            _b = s.si
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:6075  80d200       grp1 rm8,imm8
            _a = cpu.get_reg8(2)
            _b = 0x0
            _c = 1 if s.flags & CF else 0
            _r = _a + _b + _c
            cpu.set_add_flags(_a, _b, _r, 8, _c)
            cpu.set_reg8(2, _r & 0xFF)
            # 1010:6078  8cc3         mov rm16,sreg
            s.bx = (s.es) & 0xFFFF
            # 1010:607A  c1e304       shift rm16,imm8
            _r = cpu.shift(4, s.bx, 0x04, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:607D  03df         add
            _a = s.bx
            _b = s.di
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.bx = (_r & 0xFFFF) & 0xFFFF
            # 1010:607F  80d600       grp1 rm8,imm8
            _a = cpu.get_reg8(6)
            _b = 0x0
            _c = 1 if s.flags & CF else 0
            _r = _a + _b + _c
            cpu.set_add_flags(_a, _b, _r, 8, _c)
            cpu.set_reg8(6, _r & 0xFF)
            # 1010:6082  3ad6         cmp
            _a = cpu.get_reg8(2)
            _b = cpu.get_reg8(6)
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:6084  770d         ja
            cpu.instruction_count += 23
            bb = 4 if cpu.condition(0x7) else 1
        elif bb == 1:
            # 1010:6086  7204         jb
            cpu.instruction_count += 1
            bb = 3 if cpu.condition(0x2) else 2
        elif bb == 2:
            # 1010:6088  3bc3         cmp
            _a = s.ax
            _b = s.bx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:608A  7707         ja
            cpu.instruction_count += 2
            bb = 4 if cpu.condition(0x7) else 3
        elif bb == 3:
            # 1010:608C  03f1         add
            _a = s.si
            _b = s.cx
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:608E  03f9         add
            _a = s.di
            _b = s.cx
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:6090  4e           dec r16
            _old = s.si
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.si = _r
            # 1010:6091  4f           dec r16
            _old = s.di
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.di = _r
            # 1010:6092  fd           std
            cpu.set_flag(DF, True)
            cpu.instruction_count += 5
            bb = 4
        elif bb == 4:
            # 1010:6093  8cc2         mov rm16,sreg
            s.dx = (s.es) & 0xFFFF
            # 1010:6095  8bc7         mov
            s.ax = (s.di) & 0xFFFF
            # 1010:6097  f3a4         movs/cmps
            cpu.string_op(0xA4, 0xF3, None)
            # 1010:6099  fc           cld
            cpu.set_flag(DF, False)
            # 1010:609A  5f           pop r16
            s.di = cpu.pop()
            # 1010:609B  5e           pop r16
            s.si = cpu.pop()
            # 1010:609C  07           pop es
            cpu.set_sreg(0, cpu.pop())
            # 1010:609D  1f           pop ds
            cpu.set_sreg(3, cpu.pop())
            # 1010:609E  c3           ret
            cpu.instruction_count += 9
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_6053.owns_time = True

