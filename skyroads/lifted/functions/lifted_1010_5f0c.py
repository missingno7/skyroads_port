"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:5F0C  (33 instructions, 4 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x5F0C)
SIGNATURE = bytes.fromhex('558bec568b760483c60ac604006a006a')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x5F0C, 1: 0x5F19, 2: 0x5F42, 3: 0x5F46}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_5f0c(cpu, bb=0):
    """Lifted replacement for 1010:5F0C."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x5F0C, SIGNATURE, 'lifted_1010_5f0c')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:5F0C  55           push r16
            cpu.push(s.bp)
            # 1010:5F0D  8bec         mov
            s.bp = (s.sp) & 0xFFFF
            # 1010:5F0F  56           push r16
            cpu.push(s.si)
            # 1010:5F10  8b7604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:5F13  83c60a       grp1 rm16,imm8sx
            _a = s.si
            _b = 0xA
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:5F16  c60400       mov rm8,imm8
            _o = (s.si) & 0xFFFF
            mem.wb(s.ds, _o, 0x0)
            cpu.instruction_count += 6
            bb = 1
        elif bb == 1:
            # 1010:5F19  6a00         push imm8
            cpu.push(0x0000)
            # 1010:5F1B  6a0a         push imm8
            cpu.push(0x000A)
            # 1010:5F1D  ff7608       push rm16
            _o = (s.bp + 0x8) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:5F20  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:5F23  e8c8fe       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x5DEE, 0x5F26)
            # 1010:5F26  0430         add
            _a = cpu.get_reg8(0)
            _b = 0x30
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:5F28  4e           dec r16
            _old = s.si
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.si = _r
            # 1010:5F29  8804         mov
            _o = (s.si) & 0xFFFF
            mem.wb(s.ds, _o, cpu.get_reg8(0))
            # 1010:5F2B  6a00         push imm8
            cpu.push(0x0000)
            # 1010:5F2D  6a0a         push imm8
            cpu.push(0x000A)
            # 1010:5F2F  ff7608       push rm16
            _o = (s.bp + 0x8) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:5F32  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:5F35  e854fe       call
            cpu.instruction_count += 8
            emulate_call(cpu, 0x1010, 0x5D8C, 0x5F38)
            # 1010:5F38  894606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:5F3B  895608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            mem.ww(s.ss, _o, s.dx)
            # 1010:5F3E  0bc0         or
            _a = s.ax
            _b = s.ax
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:5F40  75d7         jnz
            cpu.instruction_count += 4
            bb = 1 if cpu.condition(0x5) else 2
        elif bb == 2:
            # 1010:5F42  0bd2         or
            _a = s.dx
            _b = s.dx
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:5F44  75d3         jnz
            cpu.instruction_count += 2
            bb = 1 if cpu.condition(0x5) else 3
        elif bb == 3:
            # 1010:5F46  56           push r16
            cpu.push(s.si)
            # 1010:5F47  ff7604       push rm16
            _o = (s.bp + 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:5F4A  e8e6fd       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x5D33, 0x5F4D)
            # 1010:5F4D  5e           pop r16
            s.si = cpu.pop()
            # 1010:5F4E  5e           pop r16
            s.si = cpu.pop()
            # 1010:5F4F  5e           pop r16
            s.si = cpu.pop()
            # 1010:5F50  5d           pop r16
            s.bp = cpu.pop()
            # 1010:5F51  c3           ret
            cpu.instruction_count += 5
            s.ip = cpu.pop()
            cpu.call_depth = max(0, cpu.call_depth - 1)
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_5f0c.owns_time = True
