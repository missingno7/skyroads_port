"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:3B17  (33 instructions, 9 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x3B17)
SIGNATURE = bytes.fromhex('fc601e2e8e1eca3ae8331f')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x3B17, 1: 0x3B22, 2: 0x3B29, 3: 0x3B30, 4: 0x3B3E, 5: 0x3B43, 6: 0x3B52, 7: 0x3B58, 8: 0x3B5F}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:3B22": 1}


def lifted_1010_3b17(cpu, bb=0):
    """Lifted replacement for 1010:3B17."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x3B17, SIGNATURE, 'lifted_1010_3b17')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:3B17  fc           cld
            cpu.set_flag(DF, False)
            # 1010:3B18  60           pusha
            _sp0 = s.sp
            cpu.push(s.ax)
            cpu.push(s.cx)
            cpu.push(s.dx)
            cpu.push(s.bx)
            cpu.push(_sp0)
            cpu.push(s.bp)
            cpu.push(s.si)
            cpu.push(s.di)
            # 1010:3B19  1e           push ds
            cpu.push(s.ds)
            # 1010:3B1A  2e8e1eca3a   mov sreg,rm16
            _o = 0x3ACA
            cpu.set_sreg(3, mem.rw(s.cs, _o))
            # 1010:3B1F  e8331f       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x5A55, 0x3B22)
            bb = 1
        elif bb == 1:
            # 1010:3B22  803e923100   grp1 rm8,imm8
            _o = 0x3192
            _a = mem.rb(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3B27  7407         jz
            cpu.instruction_count += 2
            bb = 3 if cpu.condition(0x4) else 2
        elif bb == 2:
            # 1010:3B29  803e923105   grp1 rm8,imm8
            _o = 0x3192
            _a = mem.rb(s.ds, _o)
            _b = 0x5
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3B2E  7522         jnz
            cpu.instruction_count += 2
            bb = 6 if cpu.condition(0x5) else 3
        elif bb == 3:
            # 1010:3B30  ff060016     inc rm16
            _o = 0x1600
            _old = mem.rw(s.ds, _o)
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            mem.ww(s.ds, _o, _r)
            # 1010:3B34  8b1ed00b     mov
            _o = 0x0BD0
            s.bx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:3B38  8b0f         mov
            _o = (s.bx) & 0xFFFF
            s.cx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:3B3A  0bc9         or
            _a = s.cx
            _b = s.cx
            _r = _a | _b
            cpu.set_logic_flags(_r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:3B3C  7405         jz
            cpu.instruction_count += 5
            bb = 5 if cpu.condition(0x4) else 4
        elif bb == 4:
            # 1010:3B3E  8306d00b02   grp1 rm16,imm8sx
            _o = 0x0BD0
            _a = mem.rw(s.ds, _o)
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ds, _o, _r & 0xFFFF)
            cpu.instruction_count += 1
            bb = 5
        elif bb == 5:
            # 1010:3B43  83c102       grp1 rm16,imm8sx
            _a = s.cx
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.cx = (_r & 0xFFFF) & 0xFFFF
            # 1010:3B46  b0b6         mov r8,imm8
            cpu.set_reg8(0, 0xB6)
            # 1010:3B48  e643         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x43) & 0xFFFF, _val, 8)
            # 1010:3B4A  8ac1         mov
            cpu.set_reg8(0, cpu.get_reg8(1))
            # 1010:3B4C  e642         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x42) & 0xFFFF, _val, 8)
            # 1010:3B4E  8ac5         mov
            cpu.set_reg8(0, cpu.get_reg8(5))
            # 1010:3B50  e642         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x42) & 0xFFFF, _val, 8)
            cpu.instruction_count += 7
            bb = 6
        elif bb == 6:
            # 1010:3B52  fe0e9231     grp4
            _o = 0x3192
            _old = mem.rb(s.ds, _o)
            _r = (_old - 1) & 0xFF
            cpu.set_incdec_flags(_old, _r, 8, dec=True)
            mem.wb(s.ds, _o, _r)
            # 1010:3B56  7807         js
            cpu.instruction_count += 2
            bb = 8 if cpu.condition(0x8) else 7
        elif bb == 7:
            # 1010:3B58  b020         mov r8,imm8
            cpu.set_reg8(0, 0x20)
            # 1010:3B5A  e620         out imm8,al
            _val = cpu.get_reg8(0)
            if cpu.port_writer:
                cpu.port_writer(cpu, (0x20) & 0xFFFF, _val, 8)
            # 1010:3B5C  1f           pop ds
            cpu.set_sreg(3, cpu.pop())
            # 1010:3B5D  61           popa
            s.di = cpu.pop()
            s.si = cpu.pop()
            s.bp = cpu.pop()
            cpu.pop()  # discard the saved SP
            s.bx = cpu.pop()
            s.dx = cpu.pop()
            s.cx = cpu.pop()
            s.ax = cpu.pop()
            # 1010:3B5E  cf           iret
            cpu.instruction_count += 5
            s.ip = cpu.pop()
            s.cs = cpu.pop()
            s.flags = cpu.pop() | 0x0002
            return
        elif bb == 8:
            # 1010:3B5F  c606923109   mov rm8,imm8
            _o = 0x3192
            mem.wb(s.ds, _o, 0x9)
            # 1010:3B64  1f           pop ds
            cpu.set_sreg(3, cpu.pop())
            # 1010:3B65  61           popa
            s.di = cpu.pop()
            s.si = cpu.pop()
            s.bp = cpu.pop()
            cpu.pop()  # discard the saved SP
            s.bx = cpu.pop()
            s.dx = cpu.pop()
            s.cx = cpu.pop()
            s.ax = cpu.pop()
            # 1010:3B66  ea53ff00f0   jmp far
            cpu.instruction_count += 4
            _ti = mem.rw(s.cs, 0x3B67)
            _ts = mem.rw(s.cs, 0x3B69)
            s.cs, s.ip = _ts, _ti
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_3b17.owns_time = True

