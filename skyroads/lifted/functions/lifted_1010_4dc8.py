"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:4DC8  (41 instructions, 14 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x4DC8)
SIGNATURE = bytes.fromhex('c80a00005657e84ff1')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x4DC8, 1: 0x4DD1, 2: 0x4DDA, 3: 0x4DE7, 4: 0x4DF1, 5: 0x4DFE, 6: 0x4E04, 7: 0x4E07, 8: 0x4E13, 9: 0x4E1B, 10: 0x4E1E, 11: 0x4E24, 12: 0x4E27, 13: 0x4E2A}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:4DD1": 1, "1010:4DDA": 2, "1010:4DE7": 3, "1010:4DF1": 4, "1010:4DFE": 5, "1010:4E04": 6, "1010:4E07": 7, "1010:4E13": 8}


def lifted_1010_4dc8(cpu, bb=0):
    """Lifted replacement for 1010:4DC8."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x4DC8, SIGNATURE, 'lifted_1010_4dc8')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:4DC8  c80a0000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0xA) & 0xFFFF
            # 1010:4DCC  56           push r16
            cpu.push(s.si)
            # 1010:4DCD  57           push r16
            cpu.push(s.di)
            # 1010:4DCE  e84ff1       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x3F20, 0x4DD1)
            bb = 1
        elif bb == 1:
            # 1010:4DD1  68c800       push imm16
            cpu.push(0xC8)
            # 1010:4DD4  687451       push imm16
            cpu.push(0x5174)
            # 1010:4DD7  e8b7f1       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x3F91, 0x4DDA)
            bb = 2
        elif bb == 2:
            # 1010:4DDA  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4DDD  68c800       push imm16
            cpu.push(0xC8)
            # 1010:4DE0  8d46f8       lea
            _o = (s.bp - 0x8) & 0xFFFF
            s.ax = _o
            # 1010:4DE3  50           push r16
            cpu.push(s.ax)
            # 1010:4DE4  e89df2       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x4084, 0x4DE7)
            bb = 3
        elif bb == 3:
            # 1010:4DE7  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4DEA  8d46f8       lea
            _o = (s.bp - 0x8) & 0xFFFF
            s.ax = _o
            # 1010:4DED  50           push r16
            cpu.push(s.ax)
            # 1010:4DEE  e8bef4       call
            cpu.instruction_count += 4
            emulate_call(cpu, 0x1010, 0x42AF, 0x4DF1)
            bb = 4
        elif bb == 4:
            # 1010:4DF1  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4DF4  6a24         push imm8
            cpu.push(0x0024)
            # 1010:4DF6  6a01         push imm8
            cpu.push(0x0001)
            # 1010:4DF8  687451       push imm16
            cpu.push(0x5174)
            # 1010:4DFB  e890fd       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x4B8E, 0x4DFE)
            bb = 5
        elif bb == 5:
            # 1010:4DFE  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4E01  e837f1       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x3F3B, 0x4E04)
            bb = 6
        elif bb == 6:
            # 1010:4E04  e8e411       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x5FEB, 0x4E07)
            bb = 7
        elif bb == 7:
            # 1010:4E07  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:4E09  6a24         push imm8
            cpu.push(0x0024)
            # 1010:4E0B  6a00         push imm8
            cpu.push(0x0000)
            # 1010:4E0D  687451       push imm16
            cpu.push(0x5174)
            # 1010:4E10  e87bfd       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x4B8E, 0x4E13)
            bb = 8
        elif bb == 8:
            # 1010:4E13  83c406       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x6
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4E16  83fe1b       grp1 rm16,imm8sx
            _a = s.si
            _b = 0x1B
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:4E19  7503         jnz
            cpu.instruction_count += 3
            bb = 10 if cpu.condition(0x5) else 9
        elif bb == 9:
            # 1010:4E1B  e90600       jmp
            cpu.instruction_count += 1
            bb = 11
        elif bb == 10:
            # 1010:4E1E  b80100       mov r16,imm16
            s.ax = 0x1
            # 1010:4E21  e90300       jmp
            cpu.instruction_count += 2
            bb = 12
        elif bb == 11:
            # 1010:4E24  b80000       mov r16,imm16
            s.ax = 0x0
            cpu.instruction_count += 1
            bb = 12
        elif bb == 12:
            # 1010:4E27  e90000       jmp
            cpu.instruction_count += 1
            bb = 13
        elif bb == 13:
            # 1010:4E2A  5f           pop r16
            s.di = cpu.pop()
            # 1010:4E2B  5e           pop r16
            s.si = cpu.pop()
            # 1010:4E2C  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:4E2D  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_4dc8.owns_time = True

