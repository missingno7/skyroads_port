"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:4201  (46 instructions, 13 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x4201)
SIGNATURE = bytes.fromhex('c806000056578b76048b04a314458b44')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x4201, 1: 0x4220, 2: 0x4223, 3: 0x422A, 4: 0x422D, 5: 0x4236, 6: 0x423C, 7: 0x424D, 8: 0x4250, 9: 0x4257, 10: 0x425A, 11: 0x4267, 12: 0x4278}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:4236": 5, "1010:4267": 11}


def lifted_1010_4201(cpu, bb=0):
    """Lifted replacement for 1010:4201."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x4201, SIGNATURE, 'lifted_1010_4201')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:4201  c8060000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            s.sp = (s.sp - 0x6) & 0xFFFF
            # 1010:4205  56           push r16
            cpu.push(s.si)
            # 1010:4206  57           push r16
            cpu.push(s.di)
            # 1010:4207  8b7604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:420A  8b04         mov
            _o = (s.si) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:420C  a31445       mov moffs
            mem.ww(s.ds, 0x4514, s.ax)
            # 1010:420F  8b4406       mov
            _o = (s.si + 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:4212  a31296       mov moffs
            mem.ww(s.ds, 0x9612, s.ax)
            # 1010:4215  a11c96       mov moffs
            s.ax = mem.rw(s.ds, 0x961C)
            # 1010:4218  2500a0       and
            _a = s.ax
            _b = 0xA000
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:421B  3d00a0       cmp
            _a = s.ax
            _b = 0xA000
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:421E  7403         jz
            cpu.instruction_count += 12
            bb = 2 if cpu.condition(0x4) else 1
        elif bb == 1:
            # 1010:4220  e91900       jmp
            cpu.instruction_count += 1
            bb = 6
        elif bb == 2:
            # 1010:4223  833e3c0000   grp1 rm16,imm8sx
            _o = 0x003C
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:4228  7403         jz
            cpu.instruction_count += 2
            bb = 4 if cpu.condition(0x4) else 3
        elif bb == 3:
            # 1010:422A  e90f00       jmp
            cpu.instruction_count += 1
            bb = 6
        elif bb == 4:
            # 1010:422D  ff7404       push rm16
            _o = (s.si + 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ds, _o))
            # 1010:4230  ff7402       push rm16
            _o = (s.si + 0x2) & 0xFFFF
            cpu.push(mem.rw(s.ds, _o))
            # 1010:4233  e8e2fa       call
            cpu.instruction_count += 3
            emulate_call(cpu, 0x1010, 0x3D18, 0x4236)
            bb = 5
        elif bb == 5:
            # 1010:4236  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:4239  e93c00       jmp
            cpu.instruction_count += 2
            bb = 12
        elif bb == 6:
            # 1010:423C  8b4402       mov
            _o = (s.si + 0x2) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:423F  8946fa       mov
            _o = (s.bp - 0x6) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:4242  bf0000       mov r16,imm16
            s.di = 0x0
            # 1010:4245  8bc7         mov
            s.ax = (s.di) & 0xFFFF
            # 1010:4247  8946fc       mov
            _o = (s.bp - 0x4) & 0xFFFF
            mem.ww(s.ss, _o, s.ax)
            # 1010:424A  e90300       jmp
            cpu.instruction_count += 6
            bb = 8
        elif bb == 7:
            # 1010:424D  83c701       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 8
        elif bb == 8:
            # 1010:4250  8b4404       mov
            _o = (s.si + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:4253  3bf8         cmp
            _a = s.di
            _b = s.ax
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:4255  7203         jb
            cpu.instruction_count += 3
            bb = 10 if cpu.condition(0x2) else 9
        elif bb == 9:
            # 1010:4257  e91e00       jmp
            cpu.instruction_count += 1
            bb = 12
        elif bb == 10:
            # 1010:425A  6a00         push imm8
            cpu.push(0x0000)
            # 1010:425C  6a00         push imm8
            cpu.push(0x0000)
            # 1010:425E  ff76fc       push rm16
            _o = (s.bp - 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4261  ff76fa       push rm16
            _o = (s.bp - 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4264  e839ff       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x41A0, 0x4267)
            bb = 11
        elif bb == 11:
            # 1010:4267  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:426A  8146fa4001   grp1 rm16,imm16
            _o = (s.bp - 0x6) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0x140
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ss, _o, _r & 0xFFFF)
            # 1010:426F  8b4406       mov
            _o = (s.si + 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:4272  0146fc       add
            _o = (s.bp - 0x4) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            mem.ww(s.ss, _o, _r & 0xFFFF)
            # 1010:4275  e9d5ff       jmp
            cpu.instruction_count += 5
            bb = 7
        elif bb == 12:
            # 1010:4278  5f           pop r16
            s.di = cpu.pop()
            # 1010:4279  5e           pop r16
            s.si = cpu.pop()
            # 1010:427A  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:427B  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_4201.owns_time = True

