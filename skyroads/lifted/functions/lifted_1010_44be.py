"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:44BE  (37 instructions, 10 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x44BE)
SIGNATURE = bytes.fromhex('c80000005657a188318b168a31a38431')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x44BE, 1: 0x44DD, 2: 0x44E0, 3: 0x44F2, 4: 0x44F9, 5: 0x44FC, 6: 0x450B, 7: 0x4511, 8: 0x451F, 9: 0x4522}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:450B": 6, "1010:451F": 8}


def lifted_1010_44be(cpu, bb=0):
    """Lifted replacement for 1010:44BE."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x44BE, SIGNATURE, 'lifted_1010_44be')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:44BE  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:44C2  56           push r16
            cpu.push(s.si)
            # 1010:44C3  57           push r16
            cpu.push(s.di)
            # 1010:44C4  a18831       mov moffs
            s.ax = mem.rw(s.ds, 0x3188)
            # 1010:44C7  8b168a31     mov
            _o = 0x318A
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:44CB  a38431       mov moffs
            mem.ww(s.ds, 0x3184, s.ax)
            # 1010:44CE  89168631     mov
            _o = 0x3186
            mem.ww(s.ds, _o, s.dx)
            # 1010:44D2  8b4608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:44D5  258000       and
            _a = s.ax
            _b = 0x80
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:44D8  3d0000       cmp
            _a = s.ax
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:44DB  7503         jnz
            cpu.instruction_count += 11
            bb = 2 if cpu.condition(0x5) else 1
        elif bb == 1:
            # 1010:44DD  e91200       jmp
            cpu.instruction_count += 1
            bb = 3
        elif bb == 2:
            # 1010:44E0  8366087f     grp1 rm16,imm8sx
            _o = (s.bp + 0x8) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0x7F
            _r = _a & _b
            cpu.set_logic_flags(_r, 16)
            mem.ww(s.ss, _o, _r & 0xFFFF)
            # 1010:44E4  a18c31       mov moffs
            s.ax = mem.rw(s.ds, 0x318C)
            # 1010:44E7  8b168e31     mov
            _o = 0x318E
            s.dx = (mem.rw(s.ds, _o)) & 0xFFFF
            # 1010:44EB  a38431       mov moffs
            mem.ww(s.ds, 0x3184, s.ax)
            # 1010:44EE  89168631     mov
            _o = 0x3186
            mem.ww(s.ds, _o, s.dx)
            cpu.instruction_count += 5
            bb = 3
        elif bb == 3:
            # 1010:44F2  833e3c0000   grp1 rm16,imm8sx
            _o = 0x003C
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:44F7  7503         jnz
            cpu.instruction_count += 2
            bb = 5 if cpu.condition(0x5) else 4
        elif bb == 4:
            # 1010:44F9  e91500       jmp
            cpu.instruction_count += 1
            bb = 7
        elif bb == 5:
            # 1010:44FC  ff760a       push rm16
            _o = (s.bp + 0xA) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:44FF  ff7608       push rm16
            _o = (s.bp + 0x8) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4502  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4505  ff7604       push rm16
            _o = (s.bp + 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4508  e847f7       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x3C52, 0x450B)
            bb = 6
        elif bb == 6:
            # 1010:450B  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:450E  e91100       jmp
            cpu.instruction_count += 2
            bb = 9
        elif bb == 7:
            # 1010:4511  6a0f         push imm8
            cpu.push(0x000F)
            # 1010:4513  ff7608       push rm16
            _o = (s.bp + 0x8) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4516  ff7606       push rm16
            _o = (s.bp + 0x6) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:4519  ff7604       push rm16
            _o = (s.bp + 0x4) & 0xFFFF
            cpu.push(mem.rw(s.ss, _o))
            # 1010:451C  e86bf9       call
            cpu.instruction_count += 5
            emulate_call(cpu, 0x1010, 0x3E8A, 0x451F)
            bb = 8
        elif bb == 8:
            # 1010:451F  83c408       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x8
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 9
        elif bb == 9:
            # 1010:4522  5f           pop r16
            s.di = cpu.pop()
            # 1010:4523  5e           pop r16
            s.si = cpu.pop()
            # 1010:4524  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:4525  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_44be.owns_time = True

