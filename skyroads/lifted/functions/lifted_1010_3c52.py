"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:3C52  (32 instructions, 7 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x3C52)
SIGNATURE = bytes.fromhex('c800000056571e8b7608c1e603033684')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x3C52, 1: 0x3C7E, 2: 0x3C82, 3: 0x3C86, 4: 0x3C89, 5: 0x3C8C, 6: 0x3C94}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_3c52(cpu, bb=0):
    """Lifted replacement for 1010:3C52."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x3C52, SIGNATURE, 'lifted_1010_3c52')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:3C52  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:3C56  56           push r16
            cpu.push(s.si)
            # 1010:3C57  57           push r16
            cpu.push(s.di)
            # 1010:3C58  1e           push ds
            cpu.push(s.ds)
            # 1010:3C59  8b7608       mov
            _o = (s.bp + 0x8) & 0xFFFF
            s.si = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:3C5C  c1e603       shift rm16,imm8
            _r = cpu.shift(4, s.si, 0x03, 16)
            s.si = (_r) & 0xFFFF
            # 1010:3C5F  03368431     add
            _o = 0x3184
            _a = s.si
            _b = mem.rw(s.ds, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.si = (_r & 0xFFFF) & 0xFFFF
            # 1010:3C63  8a5e0a       mov
            _o = (s.bp + 0xA) & 0xFFFF
            cpu.set_reg8(3, mem.rb(s.ss, _o))
            # 1010:3C66  b800a0       mov r16,imm16
            s.ax = 0xA000
            # 1010:3C69  8ec0         mov sreg,rm16
            cpu.set_sreg(0, s.ax)
            # 1010:3C6B  8e1e8631     mov sreg,rm16
            _o = 0x3186
            cpu.set_sreg(3, mem.rw(s.ds, _o))
            # 1010:3C6F  8b4606       mov
            _o = (s.bp + 0x6) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:3C72  b94001       mov r16,imm16
            s.cx = 0x140
            # 1010:3C75  f7e1         grp3 rm16
            _v = s.cx
            _result = (s.ax & 0xFFFF) * (_v & 0xFFFF)
            s.ax = _result & 0xFFFF
            s.dx = (_result >> 16) & 0xFFFF
            _carry = s.dx != 0
            cpu.set_flag(CF, _carry)
            cpu.set_flag(OF, _carry)
            # 1010:3C77  034604       add
            _o = (s.bp + 0x4) & 0xFFFF
            _a = s.ax
            _b = mem.rw(s.ss, _o)
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:3C7A  8bf8         mov
            s.di = (s.ax) & 0xFFFF
            # 1010:3C7C  b208         mov r8,imm8
            cpu.set_reg8(2, 0x8)
            cpu.instruction_count += 17
            bb = 1
        elif bb == 1:
            # 1010:3C7E  ac           stos/lods/scas
            cpu.string_op(0xAC, None, None)
            # 1010:3C7F  b90800       mov r16,imm16
            s.cx = 0x8
            cpu.instruction_count += 2
            bb = 2
        elif bb == 2:
            # 1010:3C82  d0c0         shift
            _r = cpu.shift(0, cpu.get_reg8(0), 1, 8)
            cpu.set_reg8(0, _r)
            # 1010:3C84  7303         jnb
            cpu.instruction_count += 2
            bb = 4 if cpu.condition(0x3) else 3
        elif bb == 3:
            # 1010:3C86  26881d       mov
            _o = (s.di) & 0xFFFF
            mem.wb(s.es, _o, cpu.get_reg8(3))
            cpu.instruction_count += 1
            bb = 4
        elif bb == 4:
            # 1010:3C89  47           inc r16
            _old = s.di
            _r = (_old + 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=False)
            s.di = _r
            # 1010:3C8A  e2f6         loop
            cpu.instruction_count += 2
            s.cx = (s.cx - 1) & 0xFFFF
            bb = 2 if (s.cx != 0) else 5
        elif bb == 5:
            # 1010:3C8C  81c73801     grp1 rm16,imm16
            _a = s.di
            _b = 0x138
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:3C90  feca         grp4
            _old = cpu.get_reg8(2)
            _r = (_old - 1) & 0xFF
            cpu.set_incdec_flags(_old, _r, 8, dec=True)
            cpu.set_reg8(2, _r)
            # 1010:3C92  75ea         jnz
            cpu.instruction_count += 3
            bb = 1 if cpu.condition(0x5) else 6
        elif bb == 6:
            # 1010:3C94  1f           pop ds
            cpu.set_sreg(3, cpu.pop())
            # 1010:3C95  5f           pop r16
            s.di = cpu.pop()
            # 1010:3C96  5e           pop r16
            s.si = cpu.pop()
            # 1010:3C97  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:3C98  c3           ret
            cpu.instruction_count += 5
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_3c52.owns_time = True

