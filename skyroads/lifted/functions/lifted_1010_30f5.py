"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:30F5  (48 instructions, 4 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x30F5)
SIGNATURE = bytes.fromhex('5755bace0332ff32edac8ad8c1e30236')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x30F5, 1: 0x3114, 2: 0x3119, 3: 0x3150}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches


def lifted_1010_30f5(cpu, bb=0):
    """Lifted replacement for 1010:30F5."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x30F5, SIGNATURE, 'lifted_1010_30f5')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:30F5  57           push r16
            cpu.push(s.di)
            # 1010:30F6  55           push r16
            cpu.push(s.bp)
            # 1010:30F7  bace03       mov r16,imm16
            s.dx = 0x3CE
            # 1010:30FA  32ff         xor
            _a = cpu.get_reg8(7)
            _b = cpu.get_reg8(7)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(7, _r & 0xFF)
            # 1010:30FC  32ed         xor
            _a = cpu.get_reg8(5)
            _b = cpu.get_reg8(5)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(5, _r & 0xFF)
            # 1010:30FE  ac           stos/lods/scas
            cpu.string_op(0xAC, None, None)
            # 1010:30FF  8ad8         mov
            cpu.set_reg8(3, cpu.get_reg8(0))
            # 1010:3101  c1e302       shift rm16,imm8
            _r = cpu.shift(4, s.bx, 0x02, 16)
            s.bx = (_r) & 0xFFFF
            # 1010:3104  368a875503   mov
            _o = (s.bx + 0x355) & 0xFFFF
            cpu.set_reg8(0, mem.rb(s.ss, _o))
            # 1010:3109  32ff         xor
            _a = cpu.get_reg8(7)
            _b = cpu.get_reg8(7)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(7, _r & 0xFF)
            # 1010:310B  8ae0         mov
            cpu.set_reg8(4, cpu.get_reg8(0))
            # 1010:310D  32c0         xor
            _a = cpu.get_reg8(0)
            _b = cpu.get_reg8(0)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(0, _r & 0xFF)
            # 1010:310F  ef           out dx,ax
            _val = s.ax
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 16)
            # 1010:3110  ad           stos/lods/scas
            cpu.string_op(0xAD, None, None)
            # 1010:3111  8bf8         mov
            s.di = (s.ax) & 0xFFFF
            # 1010:3113  4f           dec r16
            _old = s.di
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.di = _r
            cpu.instruction_count += 16
            bb = 1
        elif bb == 1:
            # 1010:3114  ac           stos/lods/scas
            cpu.string_op(0xAC, None, None)
            # 1010:3115  3cff         cmp
            _a = cpu.get_reg8(0)
            _b = 0xFF
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 8)
            # 1010:3117  7437         jz
            cpu.instruction_count += 3
            bb = 3 if cpu.condition(0x4) else 2
        elif bb == 2:
            # 1010:3119  32e4         xor
            _a = cpu.get_reg8(4)
            _b = cpu.get_reg8(4)
            _r = _a ^ _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(4, _r & 0xFF)
            # 1010:311B  8bef         mov
            s.bp = (s.di) & 0xFFFF
            # 1010:311D  03f8         add
            _a = s.di
            _b = s.ax
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:311F  ac           stos/lods/scas
            cpu.string_op(0xAC, None, None)
            # 1010:3120  8ad8         mov
            cpu.set_reg8(3, cpu.get_reg8(0))
            # 1010:3122  368aa78e05   mov
            _o = (s.bx + 0x58E) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ss, _o))
            # 1010:3127  ac           stos/lods/scas
            cpu.string_op(0xAC, None, None)
            # 1010:3128  8ac8         mov
            cpu.set_reg8(1, cpu.get_reg8(0))
            # 1010:312A  8ad8         mov
            cpu.set_reg8(3, cpu.get_reg8(0))
            # 1010:312C  c0e903       shift rm8,imm8
            _r = cpu.shift(5, cpu.get_reg8(1), 0x03, 8)
            cpu.set_reg8(1, _r)
            # 1010:312F  80e307       grp1 rm8,imm8
            _a = cpu.get_reg8(3)
            _b = 0x7
            _r = _a & _b
            cpu.set_logic_flags(_r, 8)
            cpu.set_reg8(3, _r & 0xFF)
            # 1010:3132  b008         mov r8,imm8
            cpu.set_reg8(0, 0x8)
            # 1010:3134  ef           out dx,ax
            _val = s.ax
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 16)
            # 1010:3135  26fe05       grp4
            _o = (s.di) & 0xFFFF
            _old = mem.rb(s.es, _o)
            _r = (_old + 1) & 0xFF
            cpu.set_incdec_flags(_old, _r, 8, dec=False)
            mem.wb(s.es, _o, _r)
            # 1010:3138  4f           dec r16
            _old = s.di
            _r = (_old - 1) & 0xFFFF
            cpu.set_incdec_flags(_old, _r, 16, dec=True)
            s.di = _r
            # 1010:3139  b4ff         mov r8,imm8
            cpu.set_reg8(4, 0xFF)
            # 1010:313B  ef           out dx,ax
            _val = s.ax
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 16)
            # 1010:313C  fd           std
            cpu.set_flag(DF, True)
            # 1010:313D  f3aa         stos/lods/scas
            cpu.string_op(0xAA, 0xF3, None)
            # 1010:313F  fc           cld
            cpu.set_flag(DF, False)
            # 1010:3140  368aa74a03   mov
            _o = (s.bx + 0x34A) & 0xFFFF
            cpu.set_reg8(4, mem.rb(s.ss, _o))
            # 1010:3145  ef           out dx,ax
            _val = s.ax
            if cpu.port_writer:
                cpu.port_writer(cpu, (s.dx) & 0xFFFF, _val, 16)
            # 1010:3146  26fe05       grp4
            _o = (s.di) & 0xFFFF
            _old = mem.rb(s.es, _o)
            _r = (_old + 1) & 0xFF
            cpu.set_incdec_flags(_old, _r, 8, dec=False)
            mem.wb(s.es, _o, _r)
            # 1010:3149  8bfd         mov
            s.di = (s.bp) & 0xFFFF
            # 1010:314B  83c728       grp1 rm16,imm8sx
            _a = s.di
            _b = 0x28
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.di = (_r & 0xFFFF) & 0xFFFF
            # 1010:314E  ebc4         jmp short
            cpu.instruction_count += 26
            bb = 1
        elif bb == 3:
            # 1010:3150  5d           pop r16
            s.bp = cpu.pop()
            # 1010:3151  5f           pop r16
            s.di = cpu.pop()
            # 1010:3152  c3           ret
            cpu.instruction_count += 3
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_30f5.owns_time = True

