"""AUTOGENERATED by dos_re.lift -- literal lift. DO NOT hand-edit in place.

Function 1010:0069  (32 instructions, 14 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet -- they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 stuck_error,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x0069)
SIGNATURE = bytes.fromhex('c80000005657833eaa41007503')
MAX_ITERATIONS = 100000000  # backstop only -- the no-progress detector below is the real guard
#: dispatch block -> its leader address (for the stuck diagnosis)
BLOCK_ADDRS = {0: 0x0069, 1: 0x0076, 2: 0x0079, 3: 0x007E, 4: 0x0084, 5: 0x0087, 6: 0x008E, 7: 0x0091, 8: 0x0097, 9: 0x009D, 10: 0x00A3, 11: 0x00B1, 12: 0x00B4, 13: 0x00B7}
PROGRESS_SAMPLE = 0xFFFF  # sample the state every 64K dispatches
#: re-entry points into this body: every INT (a blocking host service
#: rewinds IP onto it and retries), plus -- when boundary observation
#: is on -- head successors and call-site continuations (the unwind
#: re-entry rule). address -> dispatch block index; the installer
#: registers a re-entry hook at each.
RESUME_ENTRIES = {"1010:007E": 3, "1010:0084": 4, "1010:0087": 5, "1010:0097": 8, "1010:00A3": 10, "1010:00B1": 11, "1010:00B7": 13}


def lifted_1010_0069(cpu, bb=0):
    """Lifted replacement for 1010:0069."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x0069, SIGNATURE, 'lifted_1010_0069')
    s, mem = cpu.s, cpu.mem
    # A lifted function runs SYNCHRONOUSLY to completion: the interpreter
    # interleaves the outside world between instructions, but nothing
    # external can happen inside a lifted body -- so a loop WAITING for the
    # world to change waits forever. Detect that precisely rather than
    # guessing from an iteration count: a spin returns to the same block
    # with IDENTICAL registers, which is provably no progress; a long but
    # honest loop (a decompressor) advances its registers and is never
    # reported. MAX_ITERATIONS remains only as a backstop.
    _last_snap = None
    for _guard in range(MAX_ITERATIONS):
        if not _guard & PROGRESS_SAMPLE:
            _snap = (bb, s.ax, s.bx, s.cx, s.dx, s.si, s.di,
                     s.bp, s.sp, s.ds, s.es, s.flags)
            if _snap == _last_snap:
                raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                                  iterations=_guard,
                                  no_progress=PROGRESS_SAMPLE + 1)
            _last_snap = _snap
        if bb == 0:
            # 1010:0069  c8000000     enter
            cpu.push(s.bp & 0xFFFF)
            s.bp = s.sp & 0xFFFF
            # 1010:006D  56           push r16
            cpu.push(s.si)
            # 1010:006E  57           push r16
            cpu.push(s.di)
            # 1010:006F  833eaa4100   grp1 rm16,imm8sx
            _o = 0x41AA
            _a = mem.rw(s.ds, _o)
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0074  7503         jnz
            cpu.instruction_count += 5
            bb = 2 if cpu.condition(0x5) else 1
        elif bb == 1:
            # 1010:0076  e93e00       jmp
            cpu.instruction_count += 1
            bb = 13
        elif bb == 2:
            # 1010:0079  6a03         push imm8
            cpu.push(0x0003)
            # 1010:007B  e89f5f       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x601D, 0x007E)
            bb = 3
        elif bb == 3:
            # 1010:007E  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:0081  e8e73a       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x3B6B, 0x0084)
            bb = 4
        elif bb == 4:
            # 1010:0084  e81e58       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x58A5, 0x0087)
            bb = 5
        elif bb == 5:
            # 1010:0087  833eaa4108   grp1 rm16,imm8sx
            _o = 0x41AA
            _a = mem.rw(s.ds, _o)
            _b = 0x8
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:008C  7403         jz
            cpu.instruction_count += 2
            bb = 7 if cpu.condition(0x4) else 6
        elif bb == 6:
            # 1010:008E  e90c00       jmp
            cpu.instruction_count += 1
            bb = 9
        elif bb == 7:
            # 1010:0091  68c20c       push imm16
            cpu.push(0xCC2)
            # 1010:0094  e8eb5b       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x5C82, 0x0097)
            bb = 8
        elif bb == 8:
            # 1010:0097  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:009A  e91700       jmp
            cpu.instruction_count += 2
            bb = 12
        elif bb == 9:
            # 1010:009D  68d40c       push imm16
            cpu.push(0xCD4)
            # 1010:00A0  e8df5b       call
            cpu.instruction_count += 2
            emulate_call(cpu, 0x1010, 0x5C82, 0x00A3)
            bb = 10
        elif bb == 10:
            # 1010:00A3  83c402       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x2
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            # 1010:00A6  a1aa41       mov moffs
            s.ax = mem.rw(s.ds, 0x41AA)
            # 1010:00A9  ba0000       mov r16,imm16
            s.dx = 0x0
            # 1010:00AC  52           push r16
            cpu.push(s.dx)
            # 1010:00AD  50           push r16
            cpu.push(s.ax)
            # 1010:00AE  e85e62       call
            cpu.instruction_count += 6
            emulate_call(cpu, 0x1010, 0x630F, 0x00B1)
            bb = 11
        elif bb == 11:
            # 1010:00B1  83c404       grp1 rm16,imm8sx
            _a = s.sp
            _b = 0x4
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.sp = (_r & 0xFFFF) & 0xFFFF
            cpu.instruction_count += 1
            bb = 12
        elif bb == 12:
            # 1010:00B4  e83260       call
            cpu.instruction_count += 1
            emulate_call(cpu, 0x1010, 0x60E9, 0x00B7)
            bb = 13
        elif bb == 13:
            # 1010:00B7  5f           pop r16
            s.di = cpu.pop()
            # 1010:00B8  5e           pop r16
            s.si = cpu.pop()
            # 1010:00B9  c9           leave
            s.sp = s.bp
            s.bp = cpu.pop()
            # 1010:00BA  c3           ret
            cpu.instruction_count += 4
            s.ip = cpu.pop()
            return
    raise stuck_error(__name__, cpu, bb, BLOCK_ADDRS,
                      iterations=MAX_ITERATIONS, no_progress=0)


# Virtual-time preservation: this function accounts its own
# instruction_count per block, so step() must not add its dispatch +1.
lifted_1010_0069.owns_time = True

