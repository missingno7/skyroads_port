"""AUTOGENERATED by dos_re.lift — literal lift. DO NOT hand-edit in place.

Function 1010:0BAF  (22 instructions, 10 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet — they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x0BAF)
SIGNATURE = bytes.fromhex('c80000005657817e049dfe7f03')
MAX_ITERATIONS = 110000  # runaway guard: fail loud, never hang

BLOCK_COUNT = 10
BLOCKS_SEEN = set()  # basic blocks that actually executed


def coverage():
    """(len(seen), total) — how much of the function a verify run exercised."""
    return len(BLOCKS_SEEN), BLOCK_COUNT


def lifted_1010_0baf(cpu):
    """Lifted replacement for 1010:0BAF."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x0BAF, SIGNATURE, 'lifted_1010_0baf')
    s, mem = cpu.s, cpu.mem
    bb = 0
    # A lifted function runs SYNCHRONOUSLY to completion — unlike the
    # interpreter, no external I/O/timing advances between its blocks. A
    # loop that waits on hardware state (retrace/timer polls) would spin
    # forever here, so bound it and fail loud (such env-waits are poor lift
    # targets — hook them by hand instead).
    for _guard in range(MAX_ITERATIONS):
        if bb == 0:
            BLOCKS_SEEN.add(0)
            # 1010:0BAF  c8000000     enter
            interp_one(cpu, 0x1010, 0x0BAF)  # (interpreter fallback)
            # 1010:0BB3  56           push r16
            cpu.push(s.si)
            # 1010:0BB4  57           push r16
            cpu.push(s.di)
            # 1010:0BB5  817e049dfe   grp1 rm16,imm16
            _o = (s.bp + 0x4) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0xFE9D
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0BBA  7f03         jg
            bb = 2 if cpu.condition(0xF) else 1
        elif bb == 1:
            BLOCKS_SEEN.add(1)
            # 1010:0BBC  e90a00       jmp
            bb = 4
        elif bb == 2:
            BLOCKS_SEEN.add(2)
            # 1010:0BBF  817e060028   grp1 rm16,imm16
            _o = (s.bp + 0x6) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0x2800
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0BC4  7203         jb
            bb = 4 if cpu.condition(0x2) else 3
        elif bb == 3:
            BLOCKS_SEEN.add(3)
            # 1010:0BC6  e90600       jmp
            bb = 5
        elif bb == 4:
            BLOCKS_SEEN.add(4)
            # 1010:0BC9  b80200       mov r16,imm16
            s.ax = 0x0002
            # 1010:0BCC  e91600       jmp
            bb = 9
        elif bb == 5:
            BLOCKS_SEEN.add(5)
            # 1010:0BCF  817e046301   grp1 rm16,imm16
            _o = (s.bp + 0x4) & 0xFFFF
            _a = mem.rw(s.ss, _o)
            _b = 0x163
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0BD4  7d03         jge
            bb = 7 if cpu.condition(0xD) else 6
        elif bb == 6:
            BLOCKS_SEEN.add(6)
            # 1010:0BD6  e90600       jmp
            bb = 8
        elif bb == 7:
            BLOCKS_SEEN.add(7)
            # 1010:0BD9  b80100       mov r16,imm16
            s.ax = 0x0001
            # 1010:0BDC  e90600       jmp
            bb = 9
        elif bb == 8:
            BLOCKS_SEEN.add(8)
            # 1010:0BDF  b80000       mov r16,imm16
            s.ax = 0x0000
            # 1010:0BE2  e90000       jmp
            bb = 9
        elif bb == 9:
            BLOCKS_SEEN.add(9)
            # 1010:0BE5  5f           pop r16
            s.di = cpu.pop()
            # 1010:0BE6  5e           pop r16
            s.si = cpu.pop()
            # 1010:0BE7  c9           leave
            interp_one(cpu, 0x1010, 0x0BE7)  # (interpreter fallback)
            # 1010:0BE8  c3           ret
            s.ip = cpu.pop()
            return
    raise LiftRuntimeError(
        'lifted_1010_0baf' + ' exceeded MAX_ITERATIONS (unbounded internal loop -- likely an environment wait; hook it by hand)')

