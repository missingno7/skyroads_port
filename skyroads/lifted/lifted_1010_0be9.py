"""AUTOGENERATED by dos_re.lift — literal lift. DO NOT hand-edit in place.

Function 1010:0BE9  (27 instructions, 8 basic blocks)

Refactor freely: the oracle tests are the contract, not this text. Lines
marked "(interpreter fallback)" are instructions the emitter has no native
form for yet — they are exact, but they are also the to-do list.
"""
from __future__ import annotations

from dos_re.cpu import AF, CF, DF, IF, OF, PF, SF, ZF
from dos_re.hooks import self_disable_if_patched
from dos_re.lift.runtime import (LiftRuntimeError, emulate_call, emulate_far_call,
                                 emulate_int, interp_one)

ENTRY = (0x1010, 0x0BE9)
SIGNATURE = bytes.fromhex('c80200005657b980008b46042bd2f7f1')
MAX_ITERATIONS = 135000  # runaway guard: fail loud, never hang

BLOCK_COUNT = 8
BLOCKS_SEEN = set()  # basic blocks that actually executed


def coverage():
    """(len(seen), total) — how much of the function a verify run exercised."""
    return len(BLOCKS_SEEN), BLOCK_COUNT


def lifted_1010_0be9(cpu):
    """Lifted replacement for 1010:0BE9."""
    # Fail loud if the entry bytes no longer match what was lifted
    # (self-modifying code / overlay swap): raises rather than running a
    # replacement for code that is no longer there.
    self_disable_if_patched(cpu, 0x0BE9, SIGNATURE, 'lifted_1010_0be9')
    s, mem = cpu.s, cpu.mem
    bb = 0
    # A lifted function runs SYNCHRONOUSLY to completion — unlike the
    # interpreter, no external I/O/timing advances between its blocks. A
    # loop that waits on hardware state (retrace/timer polls) would spin
    # forever here, so bound it and fail loud (such env-waits are poor lift
    # targets — hook them by hand instead).
    for _guard in range(MAX_ITERATIONS):
        if bb == 0:
            BLOCKS_SEEN.add(0)
            # 1010:0BE9  c8020000     enter
            interp_one(cpu, 0x1010, 0x0BE9)  # (interpreter fallback)
            # 1010:0BED  56           push r16
            cpu.push(s.si)
            # 1010:0BEE  57           push r16
            cpu.push(s.di)
            # 1010:0BEF  b98000       mov r16,imm16
            s.cx = 0x0080
            # 1010:0BF2  8b4604       mov
            _o = (s.bp + 0x4) & 0xFFFF
            s.ax = (mem.rw(s.ss, _o)) & 0xFFFF
            # 1010:0BF5  2bd2         sub
            _a = s.dx
            _b = s.dx
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            s.dx = (_r & 0xFFFF) & 0xFFFF
            # 1010:0BF7  f7f1         grp3 rm16
            interp_one(cpu, 0x1010, 0x0BF7)  # (interpreter fallback)
            # 1010:0BF9  05a1ff       add
            _a = s.ax
            _b = 0xFFA1
            _r = _a + _b
            cpu.set_add_flags(_a, _b, _r, 16)
            s.ax = (_r & 0xFFFF) & 0xFFFF
            # 1010:0BFC  b92e00       mov r16,imm16
            s.cx = 0x002E
            # 1010:0BFF  99           cwd
            s.dx = 0xFFFF if s.ax & 0x8000 else 0x0000
            # 1010:0C00  f7f9         grp3 rm16
            interp_one(cpu, 0x1010, 0x0C00)  # (interpreter fallback)
            # 1010:0C02  8bf0         mov
            s.si = (s.ax) & 0xFFFF
            # 1010:0C04  83fe00       grp1 rm16,imm8sx
            _a = s.si
            _b = 0x0
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0C07  7c03         jl
            bb = 2 if cpu.condition(0xC) else 1
        elif bb == 1:
            BLOCKS_SEEN.add(1)
            # 1010:0C09  e90600       jmp
            bb = 3
        elif bb == 2:
            BLOCKS_SEEN.add(2)
            # 1010:0C0C  be0000       mov r16,imm16
            s.si = 0x0000
            # 1010:0C0F  e90b00       jmp
            bb = 6
        elif bb == 3:
            BLOCKS_SEEN.add(3)
            # 1010:0C12  83fe06       grp1 rm16,imm8sx
            _a = s.si
            _b = 0x6
            _r = _a - _b
            cpu.set_sub_flags(_a, _b, _r, 16)
            # 1010:0C15  7f03         jg
            bb = 5 if cpu.condition(0xF) else 4
        elif bb == 4:
            BLOCKS_SEEN.add(4)
            # 1010:0C17  e90300       jmp
            bb = 6
        elif bb == 5:
            BLOCKS_SEEN.add(5)
            # 1010:0C1A  be0600       mov r16,imm16
            s.si = 0x0006
            bb = 6
        elif bb == 6:
            BLOCKS_SEEN.add(6)
            # 1010:0C1D  8bc6         mov
            s.ax = (s.si) & 0xFFFF
            # 1010:0C1F  e90000       jmp
            bb = 7
        elif bb == 7:
            BLOCKS_SEEN.add(7)
            # 1010:0C22  5f           pop r16
            s.di = cpu.pop()
            # 1010:0C23  5e           pop r16
            s.si = cpu.pop()
            # 1010:0C24  c9           leave
            interp_one(cpu, 0x1010, 0x0C24)  # (interpreter fallback)
            # 1010:0C25  c3           ret
            s.ip = cpu.pop()
            return
    raise LiftRuntimeError(
        'lifted_1010_0be9' + ' exceeded MAX_ITERATIONS (unbounded internal loop -- likely an environment wait; hook it by hand)')

