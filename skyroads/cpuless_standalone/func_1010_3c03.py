"""AUTOGENERATED by dos_re.lift.emit_cpuless -- CPUless recovered
function for 1010:3C03 (DOS_RE 2.0 stage 2).  DO NOT hand-edit; regenerate.

Public contract (semantic): the returned dict of live outputs.
The second return value (_compat) is generated compatibility metadata
(exit-flag reproduction + virtual-time cost) consumed ONLY by the
generated CPU-ABI adapter -- it is not part of the recovered API.
"""

_PARITY = tuple((1 - bin(v).count('1') % 2) == 1 for v in range(256))


def func_1010_3c03(mem, *, sp=0, ss=0):
    _cost = 0
    cf = pf = af = zf = sf = of = df = intf = False
    _fmask = 0
    cs = 0x1010
    bb = 0
    _iters = 0
    while True:
        _iters += 1
        if _iters > 20000000:
            raise RuntimeError('CPUless dispatch spin in 1010:3C03 (block %d, cost %d): loop exceeded 20000000 iterations -- an unbounded wait (interrupt-updated flag, or a wrong port after a state divergence)' % (bb, _cost))
        if bb == 0:  # 1010:3C03
            sp = (sp - 2) & 0xFFFF
            mem.ww(ss, sp, 0x0)
            es = mem.rw(ss, sp)
            sp = (sp + 2) & 0xFFFF
            ax = mem.rw(cs, 0x3BFF)
            dx = mem.rw(cs, 0x3C01)
            intf = False
            mem.ww(es, 0x24, ax & 0xFFFF)
            mem.ww(es, 0x26, (dx) & 0xFFFF)
            intf = True
            _cost += 9
            _fmask |= 0x200
            break
        raise AssertionError('unreachable dispatch')
    _flags = ((0x1 if cf else 0) | (0x4 if pf else 0) | (0x10 if af else 0) | (0x40 if zf else 0) | (0x80 if sf else 0) | (0x800 if of else 0) | (0x400 if df else 0) | (0x200 if intf else 0)) & _fmask
    return {'ax': ax & 0xFFFF, 'dx': dx & 0xFFFF, 'es': es & 0xFFFF}, {'flags': _flags, 'fmask': _fmask, 'cost': _cost}

